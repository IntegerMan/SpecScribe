# Architecture Decision Records

This directory holds SpecScribe's Architecture Decision Records (ADRs) — short documents that
capture a significant decision, the context that forced it, the options weighed, and the
consequences we accept. They are hand-authored (not generated from `_bmad-output/`) and are
rendered into the live site by SpecScribe itself.

Each record is numbered by its filename prefix and carries a `**Status:**` line.

## Records

- [ADR 0001 — Adopt BMAD-METHOD as SpecScribe's Spec-Driven Development Framework](0001-spec-driven-development-framework.md) — **Accepted**
- [ADR 0002 — Preserve a Shared Rendering Core with Host-Neutral View Models](0002-shared-rendering-core-and-host-neutral-view-models.md) — **Accepted**
- [ADR 0003 — Keep Settings Directory-Scoped and IDE Helpers Read-Only](0003-directory-scoped-settings-and-read-only-helpers.md) — **Accepted** (on-disk shape extended by **ADR 0014**: `.specscribe` is a folder, not a flat file)
- [ADR 0004 — Preserve Cross-Surface Interaction Semantics and Host-Aware Theme Boundaries](0004-cross-surface-interaction-and-theme-contract.md) — **Accepted**
- [ADR 0005 — VS Code Webview Runtime: Core↔Extension Seam and Packaging](0005-vs-code-webview-runtime-and-packaging.md) — **Accepted** (amended by ADR 0006; CSP / "no scripts in the body" clause amended by **ADR 0012**)
- [ADR 0006 — Delivery Architecture & Distribution: JSON + SPA + npx vs. C# Static-Site + Bundled-Binary](0006-delivery-architecture-and-distribution.md) — **Accepted** (amends ADR 0005; re-affirms C# core, adds npx + optional JSON/SPA adapter, defers the pure-TS pivot; **extended by ADR 0008**)
- [ADR 0007 — Deriving a Change's Visible Surface by Reading Standard BMAD Story Sections](0007-change-surface-descriptor-for-testing-and-footprint.md) — **Accepted** (read the guaranteed BMAD sections — File List, Acceptance Criteria, Tasks, Status/Change Log — to project a change's testable footprint; no artifact changes, portable to default BMAD)
- [ADR 0008 — JSON Data-Layer as Canonical Intermediate Representation & Incremental, Event-Driven Generation](0008-json-ir-canonical-and-incremental-generation.md) — **Accepted** (extends ADR 0002 + ADR 0006; JSON IR is canonical, all surfaces project from it, generation goes incremental/event-driven; does **not** reopen the TS core port; design-now/build-later → Epic 22; the "charts stay pre-rendered SVG *in* the IR" non-goal is **amended by ADR 0013** — the IR carries chart data + component config, not SVG)
- [ADR 0009 — Front-End Framework for the Projection Layer](0009-frontend-framework-for-projection-layer.md) — **Accepted** (component-oriented presentation over the ADR 0008 IR for CSS modularity; **Vue + Nuxt 3, universal/SSR** — NFR6 baseline preserved by Nuxt prerender; relaxes ADR 0005's C#-rendering north star for the presentation layer; re-opens ADR 0006 axis B / rendering-language only, analysis stays C#; spike-gated → Epic 23, after Epic 22's IR; its chart-SVG-in-the-IR non-goal is **amended by ADR 0013**, which shrinks Story 23.4)
- [ADR 0010 — Client-Side Charting JS for Opt-In Deep-Analytics Surfaces](0010-client-side-charting-js-for-opt-in-analytics-surfaces.md) — **Accepted** (supersedes "pure SVG, no charting JS" for opt-in analytics pages only — Git Insights, Code Map colorize views; baseline/default pages stay zero-JS per NFR-5; NFR-5 reinterpreted as a required no-JS default-mode-plus-text-equivalent baseline per surface; FR31 preserved via generation-time-embedded bounded data; narrows Epic 20 Story 20.1's spike to its still-open unknowns; triggered by Story 7.11's correct-course, also affects Story 7.12) — **§1 and §6 superseded by ADR 0012** (baseline pages may now carry the chart engine; two engine families permitted), **§2 superseded by ADR 0013** (the text twin, not a default-mode chart, discharges NFR-5)
- [ADR 0011 — Directed-Graph Edge Direction: Carrier → Target](0011-directed-graph-edge-direction-carrier-to-target.md) — **Accepted** (promotes Story 19.1 §2's edge-direction rule to a cross-surface invariant: every directed-graph surface states one explicit, source-anchored direction rule, never inferred; reverse views are forward-index inversions, not new edges; the work graph uses `From`=carrier / `To`=target with `covers` the one stored-inverted "covered-by" kind; Epic 24 coupling graphs inherit the discipline — directional metrics point antecedent→consequent, symmetric metrics render undirected; memorializes an in-force convention, no behavior change)
- [ADR 0012 — Plotly.js as the Hierarchy-Chart Engine, and One Standardized Hierarchy Explorer Component](0012-plotly-hierarchy-chart-engine-and-standardized-explorer-component.md) — **Accepted** (supersedes ADR 0010 §1 + §6; Plotly vendored locally — never CDN, `file://`-safe — as the engine for the sunburst/treemap/icicle/heatmap family; ONE "Hierarchy Explorer" component becomes the only route to a hierarchy chart, with one datasource, one selector, one framing block, and an explicit `navigate` | `select` activation mode; drill-in stays a distinct affordance from activation; **two** engine families permitted — Epic 24's force-directed/chord engine is a named open question for its own spike, a third family needs an ADR; amends ADR 0005's CSP clause, landed once jointly with Story 23.4's owed amendment; SpecScribe's first third-party runtime dependency; invalidates Story 20.4 as seated; spike validates bundle size / output delta / CSP / a11y / packaging / reduced-motion as an addendum; **2026-07-29 addendum (Story 20.10):** an instance may present N server-declared VIEWS over one shared payload — leaves serialize once, a view's own structural scaffold and leaf membership are declared per view because grouping identity is a property of (leaf, view) when a rule can collapse differently per view, and the client reparents + rolls up through the SAME children-win rule rather than a second projection; first consumer is Code Map's four filter combinations, not generalized speculatively)
- [ADR 0013 — The Text Twin Is the No-JS Contract: Retiring Server-Rendered Chart SVG](0013-text-twin-is-the-no-js-contract.md) — **Accepted** (companion to ADR 0012 — Plotly cannot server-render, so **NFR-5 is amended**: information and navigation must survive JS-off, *visualization need not*, provided a server-rendered text equivalent carries the information; supersedes ADR 0010 §2; promotes the chart text twin from convention to contract — server-rendered, complete, navigable, non-color — with a hard per-surface gate that no SVG retires until its twin is audited **in a live browser with JS disabled**; amends ADR 0008 + ADR 0009 so the IR carries chart data + component config rather than pre-rendered SVG, shrinking Epic 22 and Story 23.4; the golden fingerprint stops covering charts and its replacement must land in the same story as the first SVG retirement) — **Decision 3 / Ratified decision 3 (the hard per-story gate) retired for new work by ADR 0031**, which moves twin standardization to its own epic; NFR-5's wording is unaffected
- [ADR 0014 — `.specscribe` Is a Settings Folder, Not a Flat File](0014-specscribe-settings-folder-format.md) — **Accepted** (extends ADR 0003's on-disk shape: `.specscribe` becomes a folder containing `config.json`, leaving room for future per-repository state such as incremental-build caches or run history without a second gitignored dotfile; Story 5.2's walk-up discovery and CLI/saved/default precedence are unaffected — only the storage shape changed; a pre-existing flat file is still read directly and is migrated to the folder form the next time settings are saved from it)
- [ADR 0015 — BMad Module Identity Is Open-World and Multi-Valued](0015-bmad-module-identity-open-world-and-multi-valued.md) — **Accepted** 2026-07-26 (surfaced by the Story 18.1 spike; module identity is inferred from the *skill prefix*, but every first-party BMad module except GDS prefixes its skills `bmad-`, so CIS/TEA/BMB silently resolve to `BmadMethod` and inherit BMM's glossary site-wide — GDS is correct only by coincidence; single-winner `ChoosePrimary` additionally strips **all** BMM commands from a genuine BMM repo when TEA/CIS wins a manifest-order tie, while `IsMethodPresent` still reports Detected. Decides: identify by **module code** (the `_bmad/{code}/` directory), never the skill prefix; an unrecognized code becomes a first-class **`Unmodeled`** state — its real label + an `Informational` diagnostic — rather than a `BmadMethod` fallback, with `Unknown` retained for genuine detection failure because `DiagnosticsTemplater` already renders it as "Unknown (not detected)"; BMB mints arbitrary codes, so no closed enumeration can be correct; `ModuleContext` carries the **set** of installed modules with a designated primary, generalizing `IsMethodPresent`/`IsGdsPresent`. Confirms Epic 18 **extends** `BmadArtifactAdapter` and needs **no** adapter registry — explicitly not reopening Epics 11–15's registry question. Retires the epics.md Additional-Requirements note that the command mapping is "strongly GDS-oriented … requires generalization". Ratified with all three open questions closed: Decisions 1/2/4 land in Story 18-2 as the prerequisite slice, Decision 3 defers to its own story, `ArtifactBundle.Module` stays singular)
- [ADR 0016 — The Canonical IR Carries Rendered Prose HTML](0016-ir-carries-rendered-prose-html.md) — **Proposed** (authored by Story 22.2; **amends ADR 0008 §Decision 1**, whose "AD-2's host-neutral view models plus pre-rendered SVG chart fragments" wording is now wrong in both halves — ADR 0013 §5 already amended the *chart* half, and Story 23.1 measured that the *prose* half is load-bearing: its finding that the ~889 LOC of custom Markdig renderers are a **non-risk** was proved by `v-html`-injecting whole rendered HTML and measuring `<main>` byte-identical to the static golden, and it does **not** transfer to re-modelled view models. Decides: the IR carries **Markdig-rendered prose HTML as strings** + chart data/component config + the server-rendered text twin + structured addressing (schema version, nav/drill graph, head projection, script-island declaration, per-page content hash + bytes); AD-2's view models are **not** retired — they stay the core's internal contract, only what the IR *transports* changes; ADR 0008 §Decision 4's SVG non-goal is superseded by ADR 0013 §5; the IR is `spa/` **promoted in place** — no `ir/` directory, no second capture path, and the rename is Story 22.4's call; versioned by a monotonically increasing integer that bumps on breaking shape changes only. Trade-off stated plainly: the IR is a data-**plus-markup** document for prose, the opposite of the direction ADR 0013 §5 anticipated for charts, and scoped CSS does not reach `v-html`'d markup without `:deep()`)
- [ADR 0017 — The Projection Layer's Route Space IS the IR's Path Space](0017-projection-routes-mirror-ir-paths.md) — **Proposed** 2026-07-27 (authored by Story 23.3; ADR 0009 left the projected site's *addressing* open, and ADR 0016 made it load-bearing: the IR ships whole rendered markup carrying **88,695 internal relative hrefs** across 1,049 pages. A clean extension-less route space would require rewriting every one of them, which forfeits the byte parity that made 0016's rendered-prose decision measurable. Decides: a page's route IS its IR `outputRelativePath`, verbatim, `.html` and all; **no href inside IR content is ever rewritten** — a dangling link is fixed in the emitter, never patched in the projection; the route table is generated from the manifest and **every** page prerenders, with non-migrated pages marked `data-ir-family="pass-through"` so a resolving destination is never mistaken for finished work; `crawlLinks` stays off. Two consequences accepted explicitly: Nuxt's file-based routing cannot express `.html` routes, so all IR routing goes through one `pages/[...path].vue` catch-all; and Nitro's `canWriteToDisk` substring guard silently refuses any route containing `..`, so the projection writes those pages itself. Measured by Story 23.3: `<main>` byte-identical on **189/189** migrated surfaces, IR body present verbatim on all 189, **zero** link regressions against the golden site. Cost accepted: URLs carry `.html`, and an IR path rename becomes a public URL change)
- [ADR 0018 — A Generated, Bounded, Self-Retiring Style Layer for Injected IR Content](0018-transitional-ir-content-style-layer.md) — **Proposed** 2026-07-27 (authored by Story 23.3; Story 23.2 decided the Nuxt app imports **only** a generated token bridge and never `specscribe.css` wholesale, but ADR 0016 then settled that the IR carries prose markup authored against exactly that 7,041-line monolith — leaving import-the-monolith, hand-author-7,000-lines, or ship-visually-bare, none acceptable. Decides a fourth option: `web/assets/ir-content.css`, a **second generated bridge** that is bounded by measured usage of the migrated families' markup (897 rules + 4 keyframes, **62 % smaller** than source), gated in both directions by `check:ir-content` (demonstrated red three ways, not only green), re-nested under `.ir-content` so monolith rules cannot reach a template-authored component, and **enumerated** in `ir-content.manifest.json` — that list IS the surface Story 23.4 retires. Attribute selectors deliberately do **not** bound the extraction, because they express runtime state absent from server-rendered markup and requiring them silently drops the explorer's anti-flash CSS. Extractor is hand-written against Node built-ins per ADR 0010's zero-dependency posture. Stated plainly: this **is** monolith-derived CSS and a second bridge; what makes it a transition rather than a re-import is that it is bounded, generated, scoped, counted — and that pass-through coverage, **48 %** on this repo, is reported as a number rather than implied)
- [ADR 0020 — A Module May Declare Non-Markdown Sources, Read by Exact Filename and Gated on a Schema Version](0020-module-declared-non-markdown-sources.md) — **Accepted** 2026-07-29 (authored 2026-07-27 by Story 18.5, ratified at the Epic 18 retrospective; the `*.md` source scan has been the whole definition of "what SpecScribe can see" since Epic 1, and Test Architect's `bmad-testarch-trace` writes its `PASS | CONCERNS | FAIL | WAIVED` verdict to `gate-decision.json` — never discovered, never rendered and **never diagnosed**, so the run reports `errors=0` while the single most decision-relevant artifact the module produces does not exist as far as the portal is concerned; the same silent-loss class as Story 18.4's `forge-report.html`. Widening the glob to `*.json` is rejected outright: a source root routinely holds `package.json`, lockfiles, tool caches and coverage exports, and ingesting them would make the portal's honesty about what it read strictly worse. Decides: the `*.md` scan is **unchanged** and stays the definition of a *document*; a non-markdown file is a **declared data input** read on a separate, narrower path, and only when **all four** conditions hold — module-declared (the filename appears in the producing module's own on-disk `workflow.yaml`), **exact filename** (never an extension or glob), **directory-scoped** to the module's declared output directory, and **presence-gated** on ADR 0015's module-code check, so a coincidentally-named file in a repo with no `_bmad/{code}/` reads nothing; every such source declares a `schema_version` and the parse is gated on its **MAJOR** component *before any field is touched*, an unknown major yielding `Skipped` and **no parse attempt** — because reading a fast-moving upstream project's file without a version gate turns a shape change into a *wrong* verdict on the dashboard, which is worse than an absent one; failure is always non-fatal and categorized in the existing **five**-value `AdapterDiagnosticCategory` (no sixth), and a file that will not parse is still **listed as discovered** and labelled uninterpreted rather than re-hidden. §5 states the **enrichment-only** constraint — a module's data layers on top of SpecScribe's own source-derived analysis, visibly attributed, never overriding it — and **defers to ADR 0019** when Story 18.3's record lands, so the two agree rather than compete. Explicit non-goals: not a general "ingest any JSON" seam, not a schema registry or plugin format, not permission to read a module's `config.yaml` to resolve a non-default output path (the same cross-cutting gap Story 18.4 defers for `forge_output_path`; an overridden path gets one `Informational` notice and nothing else), and not a route into the canonical IR for the raw bytes — what travels is the normalized records, exactly as AD-2 requires. Costs stated plainly: "what SpecScribe reads" is no longer one glob, per-module filename knowledge now lives in `src/` (mitigated procedurally by fixturing each pinned name with its upstream commit SHA per ADR 0015 Decision 7 — precisely how Story 18.1 got `traceability-matrix.csv` and `nfr-report.md` wrong from doc-site prose), and a minor bump inside the accepted major is trusted rather than skipped. One open question left to the owner: whether a HIGHER major should warn rather than skip)
- [ADR 0021 — Foreign Artifacts May Be Carried Verbatim Into the Portal, as Gated Dead-End Leaves](0021-carrying-foreign-artifacts-verbatim-into-the-portal.md) — **Accepted** 2026-07-29 (authored 2026-07-27 by Story 18.4, ratified at the Epic 18 retrospective; until now **every** portal page was composed by SpecScribe's own C# core — the only non-composed outputs were *assets*, not pages a reader navigates to. `bmad-forge-idea` renders `forge-report.html` on every exit, and that report IS the persona-objections/rationale content Story 18.4's AC #1 asks for; linking to `forged-idea.md` instead would leave every killed/clarified/in-progress idea with no destination, and re-rendering from the memlog would quietly replace the record with a lesser one. Decides: a foreign artifact may be carried **verbatim** — never rewritten, restyled, sanitized-by-transformation, or wrapped (wrapping nests one `<html>` inside another, Story 23.3's defect class); it is a **dead-end leaf**, not a portal page, and the linking page says so in words; carrying is gated on being **script-free** (no `<script>`, inline handler, `javascript:` URL, or `<iframe>`/`<object>`/`<embed>`) and **external-origin-free** and under a size cap, with any failure meaning not-written + no link + one `Skipped` diagnostic naming the gate; a carried artifact does **not** enter the SPA/webview capture, because `ExtractContentRegion` degrades a landmark-less page to nav-only and would ship a content-empty route beside a perfectly readable file. AD-1/AD-2 are **scoped, not broken** — the derived *information* still travels as one host-neutral view model; only a byte-identical copy of somebody else's document is carried. The line: SpecScribe never composes markup it did not author, and never claims authorship of markup it merely copied. Numbering: `0019` is claimed-but-unwritten by BOTH Story 18.3 and Story 22.3, `0020` is pre-claimed by Story 18.5, so `0021` is the first uncontested slot. One open question left to the owner: `Skipped` vs `Unsupported` for the gate's diagnostics)
- [ADR 0022 — Node Is a Build-Time Toolchain and a Generate-Time Runtime, Never a Shipped Toolchain](0022-node-is-a-build-toolchain-and-a-generate-time-runtime.md) — **Proposed** 2026-07-27 (authored by Story 23.5; answers the question ADR 0009 §:66/:74 left as a spike-owned unknown and Story 23.1 called "the single biggest open question in Epic 23". 23.1 stated it as a binary — client-rendered SPA (forfeits the PRD's NFR-5) *or* Node at run time (forfeits self-containment) — and that binary is **false**: it conflates the toolchain that BUILDS the projection layer with the runtime that RENDERS with it. Measured asymmetry: the build toolchain is **201.9 MB** with **14 native `.node` bindings** (Rolldown, Oxc, Rollup, LightningCSS) requiring `process.dlopen`; the shipped runtime artefact is **3.78 MB of pure JavaScript with ZERO native bindings** — 53× smaller and the only half that needs to exist at generate time. Proven by the two-IR experiment AC #4 required and nobody had ever run: ONE artefact, built with **no IR present at all**, isolated to a directory containing only `.output/`, rendered **1,056/1,056** routes of this repo and 32/33 of a different project via `SPECSCRIBE_IR_DIR`, at ~4 ms/route — a full 1,056-route pass in **6.3 s** against `nuxt generate`'s 25–30 s and the spike's ~130 s cold. Decides: the toolchain runs in CI/package time only and is never distributed; what ships is the project-independent `.output/` from `npm run build:package`; SpecScribe drives the prerender one request per route from the manifest it just emitted, never `nuxt generate`; the IR resolves at SERVER RUNTIME (verified by reading the built bundle — `process.env.SPECSCRIBE_IR_DIR` survives verbatim with all three `readFileSync` sites intact), which preserves 23.2's 1.00× payload measurement because "module scope" in a prebuilt artefact IS runtime; per channel, **npx and the VSIX already run on Node by construction** and the **standalone binary requires Node as a documented prerequisite** with an actionable startup error (owner decision — bundling a runtime at +50–100 MB/RID kept only as an escape hatch); asset URLs are rewritten page-relative because `app.baseURL: './'` is wrong for every non-root page; `web/` enters CI with drift gates + Vitest coverage; and Nuxt 3.21.9 → **4.5.1** is absorbed here (EOL 2026-07-31), holding 23.3's contract exactly at **190/190 byte-identical `<main>`, 190/190 verbatim, 0 link regressions**. Two findings recorded rather than smoothed over: a prebuilt artefact carrying prerendered pages returned **project A's dashboard for project B with HTTP 200**, because Nitro serves `public/` ahead of the SSR route — so `SPECSCRIBE_PACKAGE_BUILD=1` now empties the route table and the harness refuses such an artefact; and `DashboardSurface.vue` hard-throws on any project whose dashboard has no Hierarchy Explorer, raised to Story 23.3 rather than patched here. **AMENDS ADR 0006 §Decision's "self-contained packaging … stand[s]" clause**: the binary stays self-contained with respect to .NET, but acquires an external Node runtime prerequisite — stated rather than left to drift. ADR 0006's NFR6 ruling is **upheld in full**; nothing client-renders, and 23.3's `noScripts: true` is asserted per route by the experiment. Unblocks Story 23.4; binds 16.1/16.3/16.4)
- [ADR 0023 — An Analysis Observation Is a SARIF Profile, Parallel to SpecScribe's Own Diagnostics](0023-agent-facing-analysis-observation-contract.md) — **Accepted** 2026-07-28 (authored by Story 25.3 and **ratified by the owner in the same dev pass**, because six downstream stories bind to it; the one findings model **both** Epic 25 and Epic 26 bind to, so neither invents a Sonar-shaped or BMad-shaped one the other must work around. Not a blank page: SpecScribe already ships `DiagnosticSeverity`/`DiagnosticNotice`/`AdapterDiagnostic`/`DiagnosticAnchorRoot` plus a JSON-lines stderr channel into VS Code's Problems panel — and the reuse case **loses on a measured fact**, that `Commands.SerializeDiagnostics` is **two-level** (`Info` silently becomes `"warning"`), so reuse means either mislabelling every `note` or widening a contract the extension already consumes. Decides: the record is an **`AnalysisObservation`**, deliberately NOT "finding" — `## Review Findings` is a *parsed* story section rendered as an `<h3>` on **every** story page, exactly where Story 26.5 wants to put analysis results, the same collision `ArtifactCoverage` has with FR42's "coverage"; it is **parallel** to `DiagnosticNotice`, split on **subject** (SpecScribe's own run vs the user's code) plus lifetime and provenance; and it is a **named profile of SARIF 2.1.0** (OASIS Standard + Errata 01) rather than *being* SARIF (no epic/story vocabulary; **2.6×** the bytes at 1,793 B/result vs 678; and a `result` is not self-describing — it carries only a `ruleIndex` into an out-of-line rule catalogue) or diverging from it (forfeits Roslyn/GitHub/SonarQube interop for nothing). The normalized scale **IS** SARIF's `level` enum verbatim — `none`/`note`/`warning`/`error` — making the raw-SARIF direction lossless on severity, with a **mandatory text label in the payload** so UX-DR17 is satisfied by the contract rather than by rendering convention, and `result.kind` pinned to `fail` rather than left undefined. Three measured facts drove it: Sonar's **two severity axes disagree on 54.6 %** of 1,466 live issues (800 land on different normalized levels via legacy vs `impacts[]`, so the axis is pinned to MQR and the legacy values ride along verbatim); **`impacts` is an array populated TODAY** (14 issues carry two pairs — and the `impactSeverities` **facet counts issues, not pairs**, so it is structurally incapable of revealing this, which is how a scalar field looks safe and is not); and the only code→planning join **amplifies ~10×** (1,572 attached observations → **15,758** story edges, mean fan-out **10.02**, `specscribe.css` → **64 stories**, `SiteGenerator.cs` → **18 of 19 epics**). Attachment is therefore **labelled, never asserted**: a mandatory non-nullable `basis` (`deep-git-commit-mining`/`unavailable`/`none`), `confidence` never `exact`, and `entityCount` exposing the fan-out — because `--deep-git` gates both call sites so a **default run has NO join at all (100 % unattached)**, and deep git has already dropped surfaces at `errors=0`, making "no attachment", "never computed" and "silently failed" the same empty array otherwise. **`requirement` is NOT a key** (two hops, epic-granular, on top of the 10×) and the **Epic 19 work graph cannot be the join** (no file nodes — and no requirement nodes either). Provenance is **revision-first and fails closed**: the live timestamp read "an hour ago" while the analysis sat **2 commits behind** `HEAD`, so `isStale` defaults true when uncomputable. Channels **split by consumer because the constraints do**: 25.4 gets a **sharded gitignored digest** in the ADR 0014 folder (8.9 KB index + median 3.7 KB shards vs a 1.49 MB whole, since the use case is "the files I am about to touch") which cannot move the golden fingerprint; **Epic 26 gets the IR field**, which moves it — forbidden by 25.4, expected by 26.4; Sonar's **official MCP server is adopted as a complement, never as the contract** (zero code, supports Cloud, documents Claude Code — and delivers *Sonar's* model, cannot see raw compiler output, cannot attach to planning entities, dies offline, needs a token); a SpecScribe-emitted MCP surface is **deferred to its own ADR**. Proven on a genuinely independent second source class — **834 raw Roslyn SARIF results** from `dotnet build -p:ErrorLog=` that never touched Sonar, reconciling to Sonar's imports (CA1861 339 vs 338) while carrying **2 rules Sonar never sees**, against **37 `csharpsquid` rules with no raw analogue**. Costs stated plainly: a **fifth** severity vocabulary in a repo that has four; Sonar taxonomy (`cleanCodeAttribute`, `effort`/`debt`) dropped rather than modelled; the single **BLOCKER invisible** at normalized granularity, surviving only in `severity.provider`; and the 10× fan-out **exposed rather than solved** — `entityCount` gives every surface the data to bound, but the bounding rule is Story 26.5's design decision and the owner's to approve. **Story 26.2 consumes this record and must not define a second** — if its posture cannot supply an analysis revision it must amend this ADR, not work around it. Explicit non-goals: ingestion posture and credentials (26.2, incl. NFR-3 local-first), any portal surface (26.1, 26.4–26.6), any new runtime or outbound call, and **coverage** — a per-file metric with no rule identity, message, severity or location, so Epic 27/FR42 stays separate with an *uncovered-lines range* view named as the one real edge for it to decide rather than inherit)
- [ADR 0024 — The SPA and the VS Code Webview Are Filtered Projections of ONE Region Seam](0024-spa-and-webview-are-filtered-projections-of-one-region-seam.md) — **Proposed** 2026-07-28 (authored by Story 22.4; makes ADR 0008 §Decision 2's "co-equal projections" true rather than aspirational — the webview was in fact a **second independent builder**, not a projection. `BuildSpaBundle` and `RenderWebviewSurfaces` were two ~200-line methods that each rebuilt the same prelude, iterated the epics family with the same rules, and sliced captured pages with the same `CapturedNavMarkup` + `ExtractContentRegion` pair; the cost was already being paid, since Story 22.2's 5-anchor `codeItemHref` fix had to be applied **twice** with an apology comment at both call sites. Decides: **one** region seam — `BuildSurfacePrelude` + `BuildFamilySurfaces` + `CapturedRegions` — of which both surfaces are **filtered projections**; a consumer may filter and post-process but never re-derive, and the webview's entire remaining surface-specific behaviour is exhaustively its exclusion set, its degrade skip, its island strip, its `SourcePath` join, its `WrapDocument` entry document and its `BuildOutline` sidebar tree (**six** — the original wording said five and omitted `BuildOutline`, corrected by the story's code review, which also moved the exclusion set INSIDE `CapturedRegions` as a skip predicate so an excluded page is never sliced). The SPA/webview **degrade asymmetry is deliberate and preserved** (the SPA keeps a nav-only page, the webview drops it — a browser tab is escapable, a status panel claiming "links work" is not), now carried as a `Degraded` flag computed at the slice rather than by a `ReferenceEquals` check any consumer could silently break. The webview is **NOT** a Nuxt consumer (Decision 4; ADR 0005's runtime contract unchanged, Story 23.4 AC #3 keeps this path). Two divergences were **decided rather than accreted**: the JSON-island strip now applies to captured surfaces too — `impact-map.html` shipped a `HierarchyExplorer` island the registered `data-island` exception claimed the webview did not carry, invisible because the no-script test ran without capture — and the story-fragment `try/catch` now covers the SPA, which previously would abort an entire emit where the HTML path degrades to a placeholder. Decision 6 fixes the IR's **two region shapes** at the emitter: `ExtractContentRegion` slices from the wayfinding band's OUTERMOST marker, so all **594 of 1,400** previously element-unbalanced regions become balanced and `web/ir/adapter.ts`'s repair-and-throw is **deleted** — a repair that can no longer fire is a second, drifting truth about a boundary the emitter owns. That is a `schemaVersion` **1 → 2** bump per ADR 0016 §Decision 5, with both consumer constants moved in the same change because the adapter only `console.warn`s on mismatch. Measured: **828/828** webview surfaces identical in set and emission order with a byte-identical `entryDocument`; **1,350 of 1,407** static pages byte-identical, the 57 movers being 46 story pages + 9 epic pages + `work-graph.html`, all the sanctioned work-inventory fix; golden fingerprint **unmoved**. ⚠️ Consequence 23.4 must read: the slicers are **not** dead code — `_spaCapture`, `CapturedNavMarkup` and `SpaDelivery.Extract*` remain the IR's producer for ~1,200 of 1,400 pages until 23.4 replaces them, so reading ADR 0016 §Decision 4's grant as permission to delete them breaks the IR for most of the site. Explicit non-goals: not the full templater inversion, not retiring `HtmlRenderAdapter` (23.4), not the incremental engine (22.5), not a delta channel (22.6), and not a link-graph cleanup — the 592 inherited dangling links stay faithfully reproduced per ADR 0017 §Decision 2)
- [ADR 0025 — `retired` Is a Terminal Story Stage, and the Two Status Classifiers Are Deliberately Asymmetric](0025-retired-is-a-terminal-story-stage-in-both-classifiers.md) — **Proposed** 2026-07-28 (authored by Story 8.9; **amends Story 8.2's canonical lifecycle**. Story 8.2 built two classifiers over one CSS vocabulary on purpose — `ForSprint` reads a **closed set** of `sprint-status.yaml` values, `ForStatus` reads **free text** from a story artifact's `Status:` line — and that split is kept. The defect was narrower: a word promoted to first-class in one was never promoted in the other, so a story file reading `Status: retired` (Story 22.3, retired 2026-07-27) fell through to `unrecognized`. Five consequences, the load-bearing one being that `ForEpic` gated on `All(c => c == "done")`, so **an epic that retired a story could NEVER read done** — Epic 22 permanently short of it regardless of how much shipped, propagating into `RequirementsParser.DeriveStatus`. Decides: `retired` is a **canonical stage reachable from BOTH classifiers** and **TERMINAL**, joining `StoryStages` immediately after `done` and `EpicStages` for the same reason `unrecognized` is already there — with the rule that a stage added to a partition list **must** gain its label arm in the same edit, because both label switches end `_ => "Pending"` and a class without a word is a quieter mislabel than the one being removed. The roll-up is **done-or-retired ⇒ done, all-retired ⇒ retired**, with retired **counted** in the denominator, **neutral** for the lower tiers, and excluded from the all-unrecognized test rather than blocking it; `ForEpicWithRetrospective`'s gate keys on `done` alone, so **a fully-abandoned epic is not awaiting a retrospective**. ⚠ This **deliberately diverges** from `SprintTemplater.DeliveryWheel`, which EXCLUDES retired from its denominator `M` — the wheel asks *how much of the active plan is done*, the roll-up asks *is this epic closed*; **both are right and aligning them breaks one**. The vocabulary is **six words** — `retired`/`superseded`/`deprecated`/`cancelled`/`obsolete`/`wontfix` — authored in **exactly one place**, with `EpicsParser.RetirementKeyword` building its regex from that same array instead of its old hand-maintained three-word copy; matched on the whole normalized word **before** the token fallback (so `not-retired` stays unrecognized), with `-`, `'` and `’` collapsed so `wont fix` / `won't fix` / `won’t fix` are **supported, not merely tolerated**. ⚠ `ForSprint` stays **narrower permanently**: `FreeTextBadge` consults it first, so teaching it `superseded` would flip an ADR whose status line reads exactly "Superseded" from its muted strikethrough pill to a canonical Retired badge — latent today (every ADR reads "Accepted") and kept latent by test. `AdrAccentToken` reads a **third** vocabulary including `rejected`, deliberately not unified. **No 7th `--status-*` token** (holds Story 9.3 owner decision #1): retired shares `--status-deferred`, distinct by word and class only — and since `Icons.ForStatus("retired")` and `("deferred")` are **byte-identical by design**, an icon-only assertion is vacuous. Owning no token means every theme remap is inherited for free, so **no webview carve-out was needed** — verified, not assumed. The card stays **INLINE and visibly demoted** (owner D2), not hoisted into the collapsed retired-notices `<details>`, which collects a different mechanism entirely. Costs stated: `ForStatus` now has three ways to reach a stage and their ordering is load-bearing; the widened regex changes which epics.md comments get hoisted (today's corpus has none); **quick-dev doc frontmatter reclassifies too**, a second surface the ACs did not name; and `RenderParity.StoryCardRegex` had to loosen from an exact class match to a class-attribute match. Rejected: one unified classifier (breaks ADR status pills), dropping retired from the epic denominator (loses the all-retired case), **coercing retired to `done`** (green creep at the exact place the portal must be honest), hoisting the card, and earning a 7th token)
- [ADR 0026 — A Generated Artifact Derives From Templates and Source, Never From Project Data](0026-generated-layers-derive-from-templates-not-project-data.md) — **Proposed** 2026-07-28 (**amends ADR 0018**, which established `ir-content.css` as a bounded generated layer but never said *when* "the rules the migrated families actually use" is evaluated. The implementation answered it by harvesting classes from the IR **on disk at extraction time**, making a committed artifact a function of the project's own planning data — `sprint-status.yaml`, epic statuses, retrospective presence — alongside the stylesheet and templates. That failed in **both** directions on one run, 2026-07-28: **false drift**, where `check:ir-content` went red with `+ .epic-remaining-review` / `- .sprint-lane-empty` although neither rule had changed in `specscribe.css` (last touched `559f600` / `f7c6859`) — an epic had entered review and every sprint lane had filled; and, worse because invisible, **silent style loss**, where the committed sheet carried only **4 of the 10** lifecycle variants of `epic-remaining-*`, so the first epic to reach review rendered its dashboard tile against **no rule at all** and quietly lost `border-left-color: var(--status-review)`. No test could see it: the markup was correct and the status word present, so neither the byte-parity harness nor the a11y check had anything to catch. Decides the test — **a generated artifact belongs in the repository only if regenerating it on a different day, from the same commit, produces the same bytes** — which is the rule `ir-content-build.mjs` already applied to manifest *fields* (two rounds of whole-corpus and whole-source fields were removed under it) but never to the **derivation**, where the same defect also ships an unstyled page. Fix: where a class is drawn from a **closed domain**, seed the domain rather than the observed subset. Seeding is **self-limiting** — a rule is still carried only when EVERY class it names is present, so seeding `deferred` cannot drag `.req-card.deferred` onto a page with no `.req-card` — which is what keeps this from being a return to wholesale import. Three domains closed, all already authored as explicit C# lists: lifecycle stages (`StatusStyles.LegendStages`), stage-suffixed families (`epic-remaining-%s`, `dn-%s-item`, `sb-%s-item`, `sb-%s-sw`, `list-row-accent-%s`), and state-conditional singletons (`sprint-lane-empty`, `sprint-filter-empty`, `sprint-lane-more`, `unplanned-card`, `chart-empty`). Measured cost **865 → 873 rules, 107.2 → 108.1 KB (+0.8 %)**; `epic-remaining-*` coverage **4 → 7** variants. ⚠ The interim implementation in `web/scripts/ir-content-lib.mjs` is a **hand-maintained duplicate** of `StatusStyles.LegendStages` — the same class of defect it fixes, going stale *silently* the day an eleventh stage is added, and accepted only because it is strictly better than the omission it replaces and is one named block rather than a scatter. **Decision 4 is the durable form**: the C# side publishes its vocabularies into the IR (`manifest.vocabularies.stages`, emitted from `LegendStages`) so the extractor reads them and the duplicate is deleted — proposed rather than implemented because it adds a field to the canonical IR contract and wants a `schemaVersion` bump and ratification, not a quiet append. Rejected: **regenerate-on-red** (the treadmill — it does not fix the silent loss, it *alternates* which bug is live, demonstrated by the 2026-07-28 regeneration gaining `.epic-remaining-review` while losing `.sprint-lane-empty`); **a committed fixture IR** (deterministic by construction and genuinely attractive, but the repo has no IR fixture corpus and a fixture's realism is itself a thing that drifts — noted for revisit, not overlooked); carrying the whole stylesheet (removes the failure by removing the feature — the shape ADR 0018 exists to avoid); and widening the gate to tolerate small diffs (a gate with a tolerance teaches people to ignore it, and would have hidden the unstyled tile just as completely). Generalizes: any future generated-and-committed artifact inherits the test, and `check:tokens` already passes it)
- [ADR 0027 — Watch-Mode Rebuild Scope Is Decided by One Named Classifier, and Topology Escalates](0027-watch-rebuild-scope-is-one-classifier-and-topology-escalates.md) — **Proposed** 2026-07-28 (**operationalizes ADR 0008 §Decision 3** and `ARCHITECTURE-SPINE.md` § **AD-5**, which said watch mode "may rebuild narrowly when safe" without anything in the code ever deciding *when safe*: `FileWatcherService.RunDebouncedPass` picked a route from the changed path's FAMILY and each route rebuilt whatever it happened to own, so the scope question was answered implicitly, in five places, by omission — and no test anywhere compared an incremental route's output to a full regeneration, so the omission was unobservable. Story 22.1 measured the cost; Story 22.4 AC #5 closed the loudest half (the 56-page `RegenerateEpics` work-graph over-count, now re-measured byte-identical at no-op). Decides: **(1)** one named classifier, `SiteGenerator.ClassifyRebuildScope`, answers scope BEFORE the family question, on one rule — *a change is topology when the file's existence at fire time disagrees with whether the last completed pass rendered it* — from ground truth at fire time, never the watcher event kind, since one save emits Changed/Created/Deleted in any order before the debounce settles; `IsDataSource` keeps its load-bearing precedence. **(2)** Topology escalates through the EXISTING `RegenerateTopology`, so an escalated pass reports **one** coherent `GenerationEvent` rather than a flood of per-page events — escalation rather than per-surface invalidation being the decision itself, because Story 22.1's stranded list is explicitly a *lower bound* and a hand-maintained invalidation table rots silently exactly as the original omission did. **(3)** A full rebuild renders from source alone (`ResetDerivedStateForFullRebuild`): watch holds ONE generator for the session, so an escalated rebuild is `GenerateAll` called an n-th time on an instance that already has models, and it cleared `_docs` but not `_epicsModel`/`_requirements`/`_cadence`/`_counts`/`_progress`/`_referenceMap` or the `_artifactHrefByRepoRel` cache whose own comment claimed "built once per generation run" with nothing enforcing the per-RUN part — deleting `epics.md` and escalating still left `cadence.html` and `traceability.html` **orphaned** and the Code Map linking to `epics/story-N-M.html` pages that were gone; invisible to every existing test because they all build a FRESH generator. **(4)** Staying narrow is not permission to skip a surface: `code-map.html`/`risk-quadrant.html` are projected from the source walk and the walk carries each file's LINE COUNT, so an ordinary content save already makes them wrong — Story 22.1 filed `code-map.html` under topology only, and that was the lower bound hiding a content case. **(5)** A class may stay narrow only if `IncrementalOracleParityTests` PROVES it byte-identical to a cold `GenerateAll` of the identical tree, driven through the real `RunDebouncedPass` predicate order, folded through the `GoldenNormalization` the golden fingerprint shares — a second copy folding one extra token is a hole in whichever gate lacks it, and Story 22.1's private copy had already drifted. ⚠ Costs the watch log its most honest line: deleting `epics.md` now reports `<directory change>` instead of "epics.md removed; N stale page(s) deleted". That exemption was TRIED first and diffed — the bespoke Story 5.3 AC #3 teardown left **16 stale, 3 missing**, the missing ones being story artifacts that a full rebuild renders as ordinary docs once `epics.md` is gone, which no teardown *of the epics family* could produce. `ClearEpicsFamilyOutputs` and its 8 tests are untouched and still reachable through `RegenerateEpics` directly. Rejected: **per-surface invalidation** (rots silently, fails as a stale page nobody notices); **escalate everything** (correct, and surrenders the whole 3×–84× win on the commonest operation); and **converging `GenerateAll` onto the incremental routes** (it is coherent BY CONSTRUCTION, which is exactly what qualifies it as the oracle — changing it changes the measuring instrument))
- [ADR 0028 — Delta Transport Is a Sidecar and a Stream, Never a Server](0028-delta-transport-is-a-sidecar-and-a-stream-never-a-server.md) — **Accepted** 2026-07-29 (**operationalizes ADR 0008 §Decision 3** — "emits IR deltas suitable for a future watch / client-server transport" — and **resolves its deferred §Consequences question as a NO**: no long-lived process, no listener, no port. Ships exactly `ARCHITECTURE-SPINE.md` § **AD-8**'s two named clauses and no third — **sidecar polling** via `spa/delta.json` written beside the manifest on each watch-mode regen, and **extension host push** via delta frames on the NDJSON stdout channel `webview --serve` has streamed since Story 6.4. The defect closed is live and measured: that channel re-shipped a **~8 MB whole-site payload** for a one-character edit. Story 22.2's per-page `contentHash` is what unblocked it — diff manifest N against N−1 and the delta falls out, with no incremental engine underneath. Measured on this repo: a `GenerateOne` single-file content edit costs **2.72 % of the full IR / 4.09 % of the full webview payload**, stable across two runs, against Story 22.1's **25.3–39.9 %** at chunk granularity. Decides: **(1)** two transports, zero network surface, and a `specscribe serve` HTTP/SSE server explicitly OUT of scope pending its own ADR. **(2)** The delta document is a versioned contract carrying BOTH its own `deltaSchemaVersion` and the IR `schemaVersion` it was computed against; `SpaDelivery.SchemaVersion` is NOT bumped, a new file being additive. **(3)** Watch-mode only — gated on `EmitDeltaSidecar`, never on `--spa`, because the document carries a wall clock and a cold build must stay byte-reproducible (NFR9) — and written temp-then-`File.Move` since it is the one IR file designed to be POLLED while written. **(4)** Degrade to `full` on any untrustworthy basis, enforced inside `BuildDelta` so no caller can forget one. **(5)** ⚠ The topology-escalation signal is a FLAG the route sets on itself, never the `trigger` label — live verification caught a concurrent save overwriting that label between `RegenerateTopology` setting it and the emit reading it, producing `"full": false` while the watch log printed `<directory change> full rebuild`; `FileWatcherService` fires one debounce Timer per changed path on its own thread, so the label is racy BY CONSTRUCTION and correctness never reads it. **(6)** ⚠ The basis advances at the EMIT seam, NOT on the reported outcome — `RegenerateFromDataSource` calls `GenerateAll()` on its first line and only then decides what to report, so an unparseable `sprint-status.yaml` returns `Skipped` HAVING ALREADY REWRITTEN THE WHOLE IR; a basis gated on the outcome would emit a false *unchanged*, the one failure worse than a false *changed*. The NDJSON channel keeps the opposite rule for the opposite reason — two channels, two bases, two different correct answers. **(7)** The first NDJSON frame stays byte-identical to `SerializePayload`'s output so a cold consumer needs no special case; delta frames announce themselves with `"frame": "delta"` and name their partial map `changedSurfaces`, NOT `surfaces`, so a consumer that missed the discriminator degrades to a missing key rather than silently dropping every unchanged page. **(8)** Freshness is signalled as TEXT — the "Quiet Stamp" — never by color and never by motion, in the server-rendered markup of the SPA shell and webview chrome only, never a static page. ⚠ Accepted floor: `code-map.html` carries a whole-repo LOC total and so changes on EVERY content edit (~807 KB encoded, the dominant term in every delta), which no transport change can lower. Rejected: a long-lived HTTP/SSE server (owner decision — a deployment shape ADR 0008 deferred, for no consumer that exists); chunk-granular deltas (Story 22.1's own 25.3–39.9 % numbers); a second hash function (`SpaDelivery.ContentHash` already names 22.5/22.6 as its consumers))
- [ADR 0029 — A Bounded Unscoped Layer for Shared Primitive Classes](0029-unscoped-shared-primitive-layer.md) — **Proposed** 2026-07-29 (**amends ADR 0018**, specifically its property **3 (Scoped)** — "every rule is emitted under `.ir-content`, so it cannot reach a template-authored component even by accident". That property is what made a monolith-derived layer safe, and it is also what made one case impossible: `ListRow.Chip` emits `class="list-row-chip pill"` and every visual property of that chip is `.pill`'s, but `ListRow.vue` is **template-authored**, so `.ir-content .pill` reaches injected markup and never the component. The only move left inside the old rules was to hand-retype `.pill`'s declarations in the SFC, and it drifted exactly as a second definition does: Story 23.2's 2026-07-28 re-review found **serif** instead of Courier, no letter-spacing, `0.1rem 0.55rem` instead of `0.2rem 0.7rem`, and `--parchment`/`--ink-light` instead of `--warm-white`/`--ink-faded` — inside a file whose header calls itself "the Vue counterpart of `ListRow.Render`". That review fixed the **values** and recorded that it could not remove the **copy**: dropping the properties for the class alone ships an unstyled chip, confirmed before reverting, because no channel for shared non-IR primitive classes existed. Decides that channel: a second generated sheet, `web/assets/shared-primitives.css`, emitted **UNSCOPED**. The three non-scoping properties are *strengthened*, not relaxed — **bounded by an explicit ALLOWLIST rather than by usage** (`SHARED_PRIMITIVES`, one entry today: `pill`), which is a tighter bound than the scoped layer's, and carried only when **every** class a selector names is on the list, so `.pill.status-draft` and `.pill.pill-link` stay scoped where they belong; **gated in both directions by the same `check:ir-content` run**, which names which sheet drifted (demonstrated red on a hand-edited shared rule and on an absent sheet, then green); and **enumerated from BOTH sides** — a new `sharedPrimitives` manifest block carries the rules, the allowlist and the admission test, while the moved rule *also* stays in the scoped layer's `rules` list with `carried: false` and a reason, so a rule that moved cannot silently drop off the list Story 23.4 retires. **Exactly one definition**: a shared rule is REMOVED from the scoped layer, never duplicated into both, and an unscoped rule still matches inside `.ir-content` so injected markup is unaffected — the app goes from two definitions of `.pill` (one generated, one hand-typed) to one. Admission test, because growing the list is an architectural decision: a C# primitive must emit the class **and** a template-authored Vue component must consume it; classes that only ever appear in injected markup are the scoped layer's and must not be added. ⚠ Stated plainly as the cost: **ADR 0018's containment property is no longer absolute** — an unscoped rule can by construction reach any element carrying that class anywhere in the app, and the mitigation is that the set is an allowlist of one, published and gated, not that the risk is gone. Second accepted cost: a variant of a shared class stays scoped, so a Vue component writing `class="pill pill-link"` gets the base look and not the link treatment — the all-or-nothing rule behaving conservatively, fixed only by adding `pill-link` deliberately. Cascade order (`shared-primitives.css` **before** `ir-content.css`) is belt-and-braces: `.ir-content …` is already ≥(0,2,0) against an unscoped (0,1,0) and a component's own `[data-v-…]` rule also outranks it, which is what lets `ListRow.vue` keep `flex-shrink` while inheriting the look. Rejected: **accept the copy until 23.4** (owner decision — leaves a hand-typed second definition of shared vocabulary in place for the rest of the epic, having just been caught drifting, with no gate to keep the correction correct); **invert the source of truth so `web/` owns the primitives' CSS** (plausibly the end state, but it reverses Story 23.2's locked decision while the C# renderer still writes all ~1,400 pages — Story 23.4's, after the renderer stops); **an unscoped section inside `ir-content.css`** (that sheet's banner and its ADR both promise every rule is scoped; a separate file leading with "⚠️ UNSCOPED" cannot be misread); **duplicating into both layers** (the point is to stop having two definitions); and **a hand-authored `:global(.pill)` in the SFC** (still requires typing the declarations by hand — the very second definition being removed — and `CONVENTIONS.md` §6 forbids per-SFC escapes). Also corrects a **stale clause in ADR 0018 itself**: its property 4 still describes the manifest as recording each rule's "line span in the source stylesheet", but Story 23.5 removed line spans because they moved on any unrelated `specscribe.css` edit and reddened CI with an 865-line diff while the generated sheet stayed byte-identical — rules are identified by `selector` plus `within`)
- [ADR 0030 — Epic 24's Graph Engine Is the Already-Shipped Plotly `scatter` Trace Over a Generation-Time Layout](0030-epic-24-graph-engine.md) — **Accepted** 2026-07-29 (**closes ADR 0012 §4**'s named open question — "Epic 24's graph engine is a named open question, deferred to Epic 24's own spike" — which had **no owner**, because Epic 24 contained no spike story and Stories 24.1-24.5 are all implementation stories, leaving SpecScribe's second potential runtime dependency to be picked implicitly inside Story 24.2's dev pass. Decided by **Story 24.6** on measured evidence. **ADR 0012 is EXTENDED, not superseded, and stays `Accepted`.** Decides: Epic 24's force-directed views are drawn with the **already-vendored** Plotly `scatter` trace - `mode:'lines'` for edges, `mode:'markers'` for nodes - over node coordinates **computed in C# at generation time and embedded**, at a marginal bundle cost of **zero bytes**, because `scatter` cannot be excluded from any Plotly bundle and the shipped `plotly-hierarchy.min.js` was measured to register exactly `heatmap, scatter, sunburst, treemap`. **Node position is DATA, not presentation** (the ADR 0010 §3 reading), so there is no client-side force simulation and no iterative solver in the browser; determinism is normative and its construction is specified (no `System.Random`, no set/dictionary iteration order reaching a float accumulation) and was verified **byte-identical across three separate processes**. **Filtering hides, never re-lays-out** - measured 44-75 ms with surviving nodes provably not moving - because a confidence slider was measured to produce **236 distinct edge sets** against only 17 node sets, so precompute-per-state (~108 MB, ~10 min) is not viable while a single layout is. **No new engine family and no new dependency**: ADR 0012 §4's allowance of a *second* family is left **unspent**, and a third still needs its own ADR. **Apache ECharts 6.1.0 was measured and rejected on cost-of-change, not on merit** - it is the better graph engine (per-edge `lineStyle` with 83 distinct widths vs 5 bands, a **native `chord` series** confirmed rendering live, an SVG renderer for ~4 KB gzip, and a unified bundle **566 KB smaller** than the Plotly bundle it would replace, an outcome ADR 0012's options table explicitly pre-authorised) but adopting it would reopen a **complete** Epic 20 (20.1-20.9 `done`, incl. a text-twin audit and a site-wide rollout), and two defects were found live that a config-level review would have missed: `echarts.init()` on a **zero-height container throws an uncaught TypeError** (a condition SpecScribe actively creates via the `specscribe:content-swapped` re-init seam, where Plotly survived every zero-size case), and **all geometry is animation-frame-gated** so at initial render every link path carries `d=""` and every symbol `scale(0)` **while every a11y attribute passes** - an attribute-only audit certifies a chart drawing nothing. **Cytoscape.js was eliminated on accessibility before bytes**: its DOM is 1 `<div>` + 3 `<canvas>`, **zero SVG, zero per-node elements**, so a roving-tabindex layer has nothing to attach to - a **UX-DR7 FAIL** under ADR 0013, which left no SVG beneath the chart. Cross-boundary emphasis stays **dash + width band + shape + text, never hue** (UX-DR17), and because `scatter` line style is trace-level, stroke width is **quantised into bands** - so confidence must be legible from the tooltip and text twin, never from width alone. **Story 24.5 is unchanged** and still rides Plotly `heatmap`. Names three things it does **not** resolve: Plotly has **no chord trace** so Story 24.4 must hand-draw arcs or amend this ADR (never improvise a dependency in an implementation story); retiring `Charts.ReferenceGraph`'s SVG is gated on an **ADR 0013 §3 text-twin audit that no Epic 24 story owns** — **resolved 2026-07-29 by ADR 0031: Epic 24 does not own that audit, Epic 28 does**; and `WebviewRenderAdapter.StripDataIslands` means the **webview cannot receive a graph payload today**, the same open decision Story 20.4 §4.4 left for hierarchies, which should be decided once for both. Evidence boundaries recorded: no live `file://` run, no screenshot, `Tab` traversal itself unverified, no screen-reader run, `vscode-resource:`/Electron untested - so the webview verdict is a **lower bound**.)
- [ADR 0031 — Text-Twin / JS-Off Standardization Moves to Its Own Epic, Not a Per-Story Gate](0031-text-twin-standardization-moves-to-its-own-epic.md) — **Accepted** 2026-07-29 (owner-ratified at the Epic 20 retrospective; **amends ADR 0013 Decision 3 / Ratified decision 3** — the hard per-surface gate that no SVG retires until its twin is audited complete had no durable owner once Epic 20 closed, a gap ADR 0030 had already named explicitly for Epic 24's `Charts.ReferenceGraph`. Context: the project has exactly one confirmed consumer today, and the owner's stated priority is a polished, minimal happy-path core UX before broadening — auditing every new hierarchy/graph surface's twin inline was pulling feature stories toward accessibility breadth instead of UX depth. Decides: the per-story gate is retired for new work (**NFR-5's wording is unchanged** — only the delivery sequencing moves from per-story to per-epic); twin standardization becomes the scope of **Epic 28** (seated here, unscheduled, no stories yet), which follows the same spike → component → site-wide-rollout shape Epic 20 used for the chart engine — prove the pattern on one surface, verify it live, then broaden; **resolves ADR 0030's named open item directly** — Epic 24 does not own `Charts.ReferenceGraph`'s text-twin audit, Epic 28 does; a surface shipped without a complete twin after this ADR is recorded as tracked debt owed to Epic 28, not left silent; already-audited surfaces — dashboard, story detail, Code Map, Impact Map, Git Insights per Story 20.6/20.9 — are unaffected. Accepted trade-off stated plainly: JS-off and screen-reader visitors lose information parity on any surface shipped before Epic 28 rolls out that surface's twin, extending in time a concession ADR 0013 itself already named rather than removing it.)
- [ADR 0032 — The CSP Posture After the Projection Layer: Amending ADR 0005 §4 Once](0032-csp-posture-after-the-projection-layer.md) — **Proposed** 2026-07-29 (authored by **Story 23.4**, discharging the amendment **ADR 0012 §Decision 5** required to "land **once**, not twice" — one record for both the client-charting change and the projection-layer change. **Decides: NO relaxation of the policy string.** `script-src` stays nonce-locked with no `'unsafe-inline'` and no `'strict-dynamic'`; `experimental.payloadExtraction` is untouched. Story 23.1's seeded finding — that Nuxt hydration needs `'strict-dynamic'` **plus** `payloadExtraction: false`, and that half-applying it **blanked the page (148 SVGs → 0)** — is **inapplicable rather than fixed**: Story 23.3's `routeRules: { '/**': { noScripts: true } }` removed its premise, re-measured here at whole-site scale over **1,469 IR pages** as **0 Nuxt runtime `<script>` tags** and **0 `_payload.json`** on any IR route (matched against real script TAGS, never a substring — several `code/**` pages render source that *mentions* `__NUXT__`). And the webview never sees Nuxt output anyway: 23.4 AC #3 + **ADR 0024** keep it on the C# region seam, so the relaxation would have loosened `script-src` for a consumer that does not exist. **Restates ADR 0005 §4's "the body carries no scripts of its own"** — literally false of the shipped page since the vendored ~1.2 MB `plotly-hierarchy.min.js` — as a claim about the **REGION**, promoted from observation to enforced invariant: measured **0 executable scripts** and **163 inert `<script type="application/json">` data islands** across all regions, with enforcement in three places rather than trusted (the boot marker is emitted outside the region, `WebviewRenderAdapter` strips islands, and `IrSurface.vue` **throws at build time** on an executable island rather than shipping a silently inert page). The vendored bundle is **chrome, not body**, which is precisely what a nonce'd `<script src>` is for — matching ADR 0012's own addendum. Carries the spike's boundary forward unwidened: the verdict is for the **policy string** under **header** delivery over an **HTTP-served** graph, not `<meta>`, not `vscode-resource:`, not an Electron paint — "two lines wide" was a **lower bound**. Accepted trade-offs stated: **Mermaid remains the one CSP casualty** (degrades to preformatted text, unchanged since ADR 0005), and the **"one renderer" claim is true of the SITE, not the PRODUCT** — Nuxt writes every `.html` but the webview and SPA still consume the C# region seam, so **Story 23.4 Q2 stays open**, and if the webview ever becomes a Nuxt consumer the `'strict-dynamic'` finding becomes live again: **dormant, not dead**. Ships **no code change**, which is the finding.)
- [ADR 0033 — Content-Drift Gates Are Targeted and Regenerable, Not Whole-Tree Fingerprints](0033-content-drift-gates-are-targeted-and-regenerable.md) — **Proposed** 2026-07-30 (authored by **Story 23.4** at owner decision **D7**, recording a cross-cutting testing contract rather than burying it as a story note. Context: `GoldenIrFingerprint` — the IR-level gate Story 23.4 landed to satisfy its own AC #5 — was **removed** on 2026-07-30 (`70b72ab`) after producing **three different hashes across the local box, CI-Windows and CI-Ubuntu for one identical commit**; one cause was found and stays fixed (`FallbackCodeWalk`'s unsorted directory walk, `7510a70`), a second was never identified. That was not bad luck: CLAUDE.md already has to instruct agents to bisect into a throwaway tree before trusting a moved constant, `deferred-work.md:1273` records **four consecutive stories** citing a value that was stale when written (23.4 made it five), and Epic 5 found the harness itself leaking the commit SHA — a defect a reflexive regeneration would have hidden behind a green test. **Decides: a whole-tree hash is not an acceptable shape for a NEW content-drift gate.** A gate added from here must localize its failure to a named artifact, be scoped so a sibling story elsewhere in the tree cannot turn it red, regenerate via a **command producing a reviewable diff** rather than a hex-literal bump, be proven deterministic **across machines and the CI operating systems** before pinning (two local runs is a floor — 23.4's IR fingerprint passed three local runs and still differed on three platforms), and **fail loudly rather than vacuously when its oracle disappears** (the shape `RegionCompositionCorpusProof` already implements by asserting the deep-git surfaces exist *before* trusting a delta count). Reference implementation already committed: `web/measurements/parity.json`'s **per-page sha256 over 1,469 pages**. `GoldenContentFingerprint` is **grandfathered, not blessed** — kept because it covers real output nothing else covers, with no removal scheduled, but whatever replaces it when the C# writer retires (**Story 23.6**) must take the targeted shape rather than be re-pointed at the IR as another whole-tree hash. Consequences stated plainly: **there is currently NO content-drift gate over the IR** and Story 23.6 inherits that hole; the per-page oracle costs more storage than one constant; new gates are slower to author by design; and a scoped gate admits blind spots a whole-tree hash nominally covered — the judgment recorded is that a coarse gate regenerated *unread* catches less in practice than a narrow gate that is trusted, and the answer to a blind spot is another targeted gate, not a return to hashing everything. Rejected: fixing the IR fingerprint's residual nondeterminism (open-ended CI round-trips for a gate that would keep every brittleness property); auto-regenerating fingerprints in CI (a gate that regenerates itself asserts nothing — Epic 5's exact failure); dropping content-drift gating entirely (the defects it catches — 23.4's finding 4, three layers dropping the same content; 23.3's double-wrapped `<main>` on 187 pages — passed every unit test). Wiring `measure:parity` into CI is **deferred to Story 23.6**, not rejected.)
- [ADR 0034 — The IR Is the Product; the Static Site Is Rendered From It by Node](0034-the-ir-is-the-product-and-the-site-is-rendered-from-it.md) — **Proposed** 2026-07-31, **amended 2026-08-02** (authored by **Story 23.6**, owner decisions **D1** and **D5**). **Records an inversion of the CLI's output contract**, from "a static site, with an optional IR" to "an IR, plus a site rendered from it by Node". Context: `spa/` has been the CANONICAL IR since ADR 0008/0016, Story 23.4 proved Nuxt could render the whole site from it (1,469 pages, 0 unexpected deltas; 1,276 pages migrated with 0 pass-through), and ADR 0022 established Node as a generate-time runtime — yet `--spa` still defaulted to **false**. ⚠️ The hazard that forced a decision rather than a refactor: deleting the C# writer without touching that default would have left a plain `specscribe generate` emitting a stylesheet, a script, **no pages and no IR** — an empty output root at `errors=0`. **Decides:** the IR is emitted unconditionally; `--spa` is retired as a deprecated **no-op** (kept registered so existing scripts do not fail with "unknown option", and deliberately NOT re-purposed); no C# path emits a content `.html`, while C# KEEPS composing the region because **ADR 0024** holds the webview and SPA on that seam — so **"one renderer" is true of the SITE, not the PRODUCT**; Node becomes a hard prerequisite for HTML with the consequence accepted plainly (**a user without Node cannot generate a browsable site**) and the failure loud rather than silent; and **C# stays the single writer** of `specscribe.css`, `specscribe.js`, `prism.js` and `plotly-hierarchy.min.js` (the prerender copies the artefact's assets but SKIPS any file that already exists — two writers of the same file is the drift this epic exists to end). Consequences stated with numbers: a cold generate paid **13.5 s / 9.1 ms-per-route** over 1,492 routes with the C# writer still running — ⚠️ 2.3× Story 23.5's ~4 ms/route — and **3.7 ms/route (1,213 routes in 4,492 ms) after the deletion**, confirming the gap was the duplicated C# write rather than the prerender transport; `EmitDeltaSidecar` stays gated on watch/serve so a one-shot generate remains byte-reproducible (NFR9); `GoldenContentFingerprint` is **retired** with `check:parity` over a **pinned** corpus as its successor, deliberately NOT re-pointed at the IR as another whole-tree hash per ADR 0033; ~268 test call sites move to the region and ~206 more to the IR, with the C# unit suite deliberately NOT booting Node. **A blind spot is closed rather than opened**: the previous oracle hashed `<main>` ONLY, so `<title>`, meta, the favicon, the footer, `<script src>`, the nav toggle, the Mermaid init and the Hierarchy/Graph anti-flash handshakes were in **no committed digest** — exactly the chrome this ADR deletes the C# emitter for. ⚠️ **AMENDED 2026-08-02 — Decision 6 added, and the amendment exists because that blind spot was hiding live breakage.** Executing Decision 3 revealed that `web/ir/adapter.ts` had been **scraping the generated `index.html`** for the favicon, the `?v=` cache-bust and the anti-flash boot script — making the RENDERER a consumer of the artifact this ADR deletes, inside a `try/catch` that would have dropped all three from every page **silently**. It also revealed that the renderer had **no mermaid, no relationship-graph boot/engine and no TOC tracker at all**, so those three features had been dark on the shipped portal since the prerender became the writer; `pageSha` is a renderer SNAPSHOT, so it had pinned the broken output as correct — a snapshot gate detects drift from what shipped, never what never shipped. **Decision 6:** the IR carries a site-level `chrome` block (`assetVersion`, `faviconDataUri`, and the hierarchy/graph/mermaid/TOC scripts), because "the IR is the product" is only true if the IR is SUFFICIENT to produce the site. Which pages need each script stays **DERIVED from the region** (`chromeNeeds`), never read from a flag — shipping flags would reintroduce Story 20.7's bug, where four surfaces emitted the mount point but never set the flag. Purely additive, so `SchemaVersion` is deliberately NOT bumped. Whether the C# SPA form still earns its keep is an explicit **non-goal**.)
- [ADR 0035 — The SonarCloud Quality Gate Is Inherited Deliberately, and Rule-Level Exceptions Have One Home](0035-sonarcloud-quality-gate-and-rule-decision-policy.md) — **Proposed** 2026-07-31
  (raised at **Story 25.2**'s code review, since the story's own binding clarifications scoped a proposed-ADR
  requirement only to its rejected `.editorconfig` path, while the decisions it actually locked in are
  cross-cutting CI/quality policy regardless. Context: 25.1 wired SonarCloud into CI without deciding what the
  analysis *means* — the default `Sonar way` gate (id 9) was already **red** before anyone chose it, on
  conditions including a `new_coverage` failure caused by a C#-only coverage report against mixed C#/JS/Vue new
  code, and a `days: 30` sliding new-code window that measurably went from 3,198 to 22,640 "new" lines in a
  single day as it absorbed unrelated epics on shared `main`. **Decides:** keep `Sonar way` rather than mint a
  custom gate or adopt the org's own undocumented, unapplied `Customized` gate (id 4194) — a live example of the
  server-side-drift failure mode a custom gate carries; fix coverage gaps by supplying the missing report where
  affordable (Story 23.5 did this for `web/` days later) rather than only excluding the scope; keep the `days:
  30` window with a written trigger to switch to `previous_version` at Epic 16's first release tag; leave
  `sonar.qualitygate.wait` unset until three written preconditions all hold; and give every "we don't follow this
  rule" decision exactly **one** home — `docs/SonarCloudSetup.md` § *Rule-level decisions*, enforced via
  `sonar.issue.ignore.multicriteria` on the workflow's `begin` step, never the SonarCloud UI. Numbering: `0034`
  is claimed-but-unwritten by Story 26.2's spike, so `0035` is the first uncontested slot. Motivated in part by a
  finding at 25.2's own review: without a single authoritative record, Story 25.6 had to independently
  rediscover and correct a stale claim about which rule drove `new_security_rating` that had gone stale across
  three separate files.)
- [ADR 0036 — The Webview Shell Supplies Chrome Scripts](0036-the-webview-shell-supplies-chrome-scripts.md) — **Proposed** 2026-08-01
  (authored by `spec-vscode-extension-name-latency-and-webview-sunburst`, from an owner report that "the main
  plotly sunburst isn't populating with data in the VS Extension". **Amends [ADR 0032](0032-csp-posture-after-the-projection-layer.md)
  §Decision 2's enforcement clause ONLY** — not the policy string, not Decision 1. Context: hierarchy and graph
  charts render on the webview as a panel heading and a legend describing a chart that is not there, because
  three legs are severed — the island is stripped, the mount code is not shipped, and the engine `<script src>`
  is emitted only by `HtmlRenderAdapter.Render`, which this adapter never calls. ⚠️ **None of it was ever a CSP
  limit**: the `hierarchy-chart` host exception describes its own cause as *"a SEQUENCING choice rather than a
  technical limit"*, and ADR 0030 §Good says *"the webview CSP needs no relaxation"*. **Decides:** the webview
  shell emits the engine + mount code as nonce'd `<script>` blocks — which is exactly what ADR 0032 §Decision 3
  already permits — with **no change to the policy string**; the mount logic must not be forked (a webview-only
  copy is the three-arc-renderers failure ADR 0012 exists to end); ADR 0032 §Decision 2's second enforcement
  point (*"`WebviewRenderAdapter` strips every JSON island"*) is **removed**, since the strip never enforced that
  invariant — the same sentence explicitly *permits* inert data islands, and the strip existed for **dead
  weight**, a rationale that expires once an engine can read them; `data-island` and `hierarchy-chart` host
  exceptions are **retired** and `asset.js` **narrowed**; and hierarchy charts plus Epic 24 graphs are covered
  **together**, satisfying ADR 0030's "decided once, for both". ⚠️ **Deliberately NOT a new CSP decision** — ADR
  0032 already discharged ADR 0012 §Decision 5's "landed once, not twice", and the seeded plan to write one was
  rejected as exactly the duplication that clause forbids. Costs stated rather than discovered: **+4,528,007 B**
  of islands across 167 pages and **+1,223,563 B** of engine, per an explicit **owner decision of no per-island
  budget** taken against a measured 15× cliff between `story-23-2.html` (81,884 B) and `code-map.html`
  (1,243,124 B) — so Story 20.9's 1.24 MB returns, no longer dead but still the first line item to revisit. No
  text-twin audit is owed: [ADR 0031](0031-text-twin-standardization-moves-to-its-own-epic.md) moved that to
  Epic 28. ⚠️ **Open:** ADR 0032 is itself still `Proposed` — the two want ratifying together.)
- [ADR 0038 — Framework Adapter Selection and Neutral Source-Root Discovery](0038-framework-adapter-selection-and-neutral-source-root-discovery.md) — **Proposed** 2026-08-06
  (authored by [Story 12.2](../../_bmad-output/implementation-artifacts/12-2-gsd-core-baseline-adapter-coverage.md),
  owner decisions D4/D5. **The ONE registry decision** — Epics 11, 12.3, 13, 14 and 15 inherit it and must not
  propose a second. Context: a non-BMad repository could not generate at **two independent layers** — `SiteGenerator`
  held a single hardcoded `BmadArtifactAdapter` field whose comment promised a registry "with Stories 4.3+" (those
  stories relocated into Epics 11–15, orphaning the promise), and `ForgeOptions.SourceDirName` was the literal
  `"_bmad-output"`, so a pure GSD repo threw `DirectoryNotFoundException` **before `AppliesTo` was ever called**.
  **Decides:** an ordered registry where EVERY matching adapter runs and single-valued families merge
  first-non-null-wins with every drop reported as a `Skipped` diagnostic (a single-adapter run returns that
  adapter's bundle verbatim, so no notice fires on an existing BMad project); an ordered marker set
  `.planning → .gsd → .specify → _bmad-output` where framework INSTALL markers precede BMad's OUTPUT folder — the
  one place Story 12.2's task list was followed in intent rather than to the letter, because probing the output
  folder first makes it a universal winner and would have left every GSD artifact outside the source root in the
  real reference repo, while costing nothing for a BMad-only repo, which has no other marker; and the scoped
  watch-mode epics re-ingest moves ONTO `IArtifactAdapter`, which is what makes BMad's watch output byte-identical
  **by construction** rather than by measurement (AD-5, [ADR 0027](0027-watch-rebuild-scope-is-one-classifier-and-topology-escalates.md)).
  ⚠️ **Deliberately NOT decided:** multi-ROOTED source discovery — in a two-framework repo the non-primary
  framework's loose documents do not render as pages, diagnosed rather than hidden; that is Story 4.9's AC #2.
  Also **names rather than silently does** two widenings it declines: `ModuleContext` stays BMad-typed (so the
  About-SDD "Planning docs"/"Commands" columns are structurally unfillable for any non-BMad framework — a ceiling
  stated on each framework page), and [ADR 0020](0020-module-declared-non-markdown-sources.md)'s non-markdown gate
  stays BMad-keyed, which is why GSD's `config.json` is reported as uninterpreted rather than read. **No new gate**
  per [ADR 0033](0033-content-drift-gates-are-targeted-and-regenerable.md).)
- [ADR 0039 — A Second Bounded Unscoped Layer, for Runtime-Attached Body-Level Classes](0039-runtime-attached-body-level-classes.md) — **Proposed** 2026-08-06
  (**Amends [ADR 0029](0029-unscoped-shared-primitive-layer.md)**, which carved the first exception to
  [ADR 0018](0018-transitional-ir-content-style-layer.md)'s property 3. Context: the owner's verify round found the
  sunburst had **no hover tooltip and a details rail that went blank on selection**. Neither was a JavaScript
  regression — every handler still fired. `specscribe.js` appends the shared tooltip to `document.body`, which is
  OUTSIDE `.ir-content`, so the scoped rules the extractor emits could never match it; and the rail's `display: block`
  reveal rule names `.is-related-current`, a class only JS applies, so it was pruned while its `display: none`
  sibling survived — a stylesheet that hid every card and revealed none. Latent since
  [ADR 0034](0034-the-ir-is-the-product-and-the-site-is-rendered-from-it.md) made the Nuxt pages the product; these
  rules were never in `ir-content.css` in any commit. **Decides:** a second bounded unscoped allowlist
  (`RUNTIME_BODY_CLASSES` → `web/assets/runtime-body.css`) whose admission test is **CONTAINMENT, not harvest
  visibility and not provenance** — `codemap-card*` IS harvested and still qualifies, while `ss-hierarchy-sector` IS
  runtime-applied and does NOT, because it lives inside the wrapper; the two allowlists stay separate and
  guard-tested disjoint; re-parenting the tooltip is **rejected** (its body-level placement is what keeps it above
  the sticky nav and unclipped, and its coordinates are page-space). Also makes this layer's signature silent
  failure visible: `extract:ir-content` now reports every dropped selector with the token that caused it —
  **reported, not gated**, because it is a whole-source function that would redden CI on edits that cannot move the
  layer. ⚠️ The round-trip gate **cannot** catch a bug in its own derivation — it stayed green through all four
  incidents — so `web/test/ir-content-harvest.test.mjs` pins the derivation directly, both directions.)
