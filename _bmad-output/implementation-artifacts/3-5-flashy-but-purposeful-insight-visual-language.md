---
baseline_commit: 84cee822fcd6246d8b5d6d26bcb3ebbdc6bfb3e4
---

# Story 3.5: Flashy but Purposeful Insight Visual Language

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a stakeholder consuming status quickly,
I want insight visuals to feel impressive but still actionable,
so that demos and day-to-day usage both benefit from clarity and impact.

## Acceptance Criteria

1. **Given** insight modules render charts and drill paths **When** the page loads and interactions occur **Then** transitions communicate state changes clearly **And** motion remains bounded, meaningful, and performance-safe. [Source: epics.md#Story 3.5, lines 479-491; UX-DR20; DESIGN.md "Motion philosophy"]
2. **Given** accessibility constraints apply **When** flashy visual affordances are enabled **Then** equivalent text and non-color cues remain present **And** reduced-motion settings preserve full informational meaning. [Source: epics.md#Story 3.5; UX-DR18; UX-DR8; UX-DR17; NFR6]

## Tasks / Subtasks

- [x] Task 1: Establish ONE shared, tokenized motion vocabulary (AC: #1, #2)
  - [x] Subtask 1.1: Add motion custom properties to `:root` in [assets/specscribe.css](../../src/SpecScribe/assets/specscribe.css) — a small named set, e.g. `--motion-fast` (~0.12s, the current hover/opacity feel), `--motion-entrance` (~0.6s, the chart reveal), `--motion-entrance-long` (~0.9s, the progress sweep), `--motion-ease` (`ease-out`), and `--motion-stagger` (~0.04s per-item delay unit). This is the SAME single-source discipline the six `--status-*` tokens enforce for color ([specscribe-status-token-system]): "flashy but purposeful" must be ONE deliberate vocabulary, not per-chart magic numbers scattered through the file. Do NOT add a new palette, and do NOT co-opt or reshape the `--status-*` tokens — motion tokens are a new, parallel family.
  - [x] Subtask 1.2: Route the EXISTING Story 1.5 entrance animations and hover transitions through these tokens instead of hard-coded seconds: the `no-preference` block's `progress-grow` (0.9s → `--motion-entrance-long`) and `chart-enter` (0.6s → `--motion-entrance`) at [specscribe.css:2135-2148](../../src/SpecScribe/assets/specscribe.css), and the `.sb-seg`/`.donut-seg`/card `transition:` durations ([specscribe.css:1118, :1478, :636 etc.](../../src/SpecScribe/assets/specscribe.css)) where they express the shared motion feel. Timings-only refactor — do not change which properties animate or the visual result; a stylesheet test locks the tokens' presence, not new pixels.
  - [x] Subtask 1.3: Keep the TWO paired media-query blocks — `@media (prefers-reduced-motion: no-preference)` ([:2135](../../src/SpecScribe/assets/specscribe.css)) and `@media (prefers-reduced-motion: reduce)` ([:2156](../../src/SpecScribe/assets/specscribe.css)) — as the ONLY motion seam. Everything new in this story hangs off one of these two blocks. Do not scatter new `@media (prefers-reduced-motion …)` blocks elsewhere; extend the existing pair in place (they were deliberately left side-by-side by Story 1.5 for exactly this).

- [x] Task 2: Extend bounded entrance motion to the Epic 3 insight surfaces (AC: #1, #2)
  - [x] Subtask 2.1: **Commit heatmap** (already built, [Charts.CommitHeatmap](../../src/SpecScribe/Charts.cs:468)) — give it a bounded entrance under `no-preference`: cells fade/scale in (opacity + transform only), optionally staggered left-to-right by week using `animation-delay` derived from `--motion-stagger` (cap the total so the last cell never lags more than ~0.4s — bounded, single-pass, never a loop). The heatmap is the hero "how has the work gone" visual (Story 1.5 E1); a brief reveal sells the demo without harming the read. Add a hook class if needed; do NOT alter the SVG geometry, the future-day muting, the `<title>`/aria-label, or the `:target` drill panels.
  - [x] Subtask 2.2: **Sibling Epic 3 surfaces** — the git-pulse panels (Story 3.1), the coverage panel (Story 3.3), the structure tree (Story 3.4), and the refinement funnel (Story 3.6) were each **deliberately kept static and handed their motion to THIS story** [Source: [3-3…:57](../../_bmad-output/implementation-artifacts/3-3-agent-and-workflow-structure-coverage-insights.md), [3-4…:33,:62](../../_bmad-output/implementation-artifacts/3-4-interactive-tree-views-for-project-and-artifact-structure.md)]. Apply the shared panel reveal (`chart-enter`-style opacity+transform, `--motion-entrance`) to their root containers. **Write this defensively:** hang the reveal off a shared, already-present selector (e.g. `.chart-panel`, or a small opt-in class like `.insight-reveal` you add to each surface) so that a surface which is not yet built is simply a no-op — never assume a specific sibling's markup exists (see Dev Notes "Sequencing").
  - [x] Subtask 2.3: **Refinement funnel** (Story 3.6) — if the funnel surface exists when this story is implemented, give it a staged narrowing reveal (each funnel stage settles in sequence via `--motion-stagger`) so the "how far refined" story reads as it draws. This is the single most demo-worthy Epic 3 visual for UX-DR20's "visual summaries." If 3.6 has not landed yet, leave a tokenized rule keyed to the funnel's container class ready to activate, and note it deferred in Completion Notes rather than inventing the funnel here.
  - [x] Subtask 2.4: Every entrance is **single-pass, bounded, and information-neutral** — the content is fully present and readable at rest; motion only reveals it. No looping, no infinite pulse (the one Story 1.5 exception, the reduced-motion-gated "In development" card emphasis, is already gated — do not add new pulses). [DESIGN.md "No looping animations"; UX-DR18]

- [x] Task 3: Richer but bounded interaction motion — legend emphasis + drill-path affordance (AC: #1)
  - [x] Subtask 3.1: **Interactive-legend emphasis (UXO C3)** — the item Story 1.5 explicitly deferred here ("richer interactive-legend dimming … → Story 3.5" [Source: [1-5…:115](../../_bmad-output/implementation-artifacts/1-5-dashboard-insight-polish-and-visual-truthfulness.md)]). Hovering/focusing a sunburst or donut **legend** entry emphasizes the matching-status segments and dims the rest, so a reader can isolate "show me what's in review" at a glance. Mirror the existing sunburst hover-invert grammar (Story 1.5 D3: dim siblings, emphasize target — [specscribe.css:1118](../../src/SpecScribe/assets/specscribe.css)) so it reads as the SAME language, not a new one.
  - [x] Subtask 3.2: **Prefer pure CSS.** The legend lives in a sibling `<div>` to the SVG, so cross-referencing a legend hover to `.sb-<status>` segments wants a scoped `:has()` selector on the shared panel (broadly supported in current evergreen browsers) or the general-sibling combinator. If — and only if — a pure-CSS mapping proves infeasible, extend the ONE existing sanctioned script [assets/specscribe.js](../../src/SpecScribe/assets/specscribe.js) with a minimal class toggle (legend `mouseover`/`focusin` → add an `emphasize-<status>` class on the panel; remove on out/blur). **Do NOT** add a second script file, a third-party library, a client-side drill/zoom/URL-hash engine, or dark-mode/theme code — those are permanently out of scope ([charting-is-pure-svg-no-js]; Story 1.5 guardrails).
  - [x] Subtask 3.3: The legend entries must be **keyboard-reachable** for the emphasis to satisfy "reachable without a pointer" — give each legend `<span>` a `tabindex="0"` (or wrap in a focusable element) and drive the emphasis off `:focus-within`/`focus` as well as `:hover`, matching how Story 1.5 made chart segments focus-reachable. Preserve the shared `:focus-visible` ring ([story-1-4-a11y-seams-for-1-5]).

- [x] Task 4: Accessibility & performance safety — the reduced-motion invariant (AC: #2)
  - [x] Subtask 4.1: Extend the `@media (prefers-reduced-motion: reduce)` block ([specscribe.css:2156](../../src/SpecScribe/assets/specscribe.css)) so it **neutralizes every new animation and transform** introduced here (the `*`-wildcard duration collapse already covers most; add explicit `transform:none`/`animation:none` for any new hover-transform or staggered entrance, exactly as 1.4/1.5 did for the card lifts). The `reduce` path is a hard invariant, not a nicety: with motion off, the insight surfaces must lose ZERO information (AC #2). [UX-DR18; UX-DR8; Story 1.4 AC #2]
  - [x] Subtask 4.2: Verify **full informational parity** with motion off: status stays color + text label + shape (never color-only, UX-DR17/NFR6); every count/fraction/headline is real text at rest; the legend-emphasis affordance degrades to the plain, always-visible legend + segment `<title>`/aria-labels (emphasis is enhancement, never the only way to read a status). No affordance in this story may be the sole carrier of any fact.
  - [x] Subtask 4.3: **Performance-safe by construction:** animate only compositor-friendly properties (`opacity`, `transform`) — never `width`/`top`/layout in the new work (the existing `progress-fill` width sweep is grandfathered and bounded). Bounded single-pass durations, no loops. Use `will-change` only if a measured jank problem demands it, and scope it tightly (an always-on `will-change` on many cells is itself a perf cost). Output stays static-host-safe (GitHub Pages) with no new runtime dependency; insight motion is additive and never blocks baseline generation ([ARCHITECTURE-SPINE.md#AD-4](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md:58)).
  - [x] Subtask 4.4: Preserve every existing a11y seam untouched — the `:focus-visible` ring, whole-chart + per-segment `aria-label`s and `<title>`s, skip link, single `<main>` landmark, progressbar ARIA, and the no-JS `<title>` tooltip fallback all remain. **Extend, never overwrite** ([story-1-4-a11y-seams-for-1-5]).

- [x] Task 5: Test coverage (AC: #1, #2)
  - [x] Subtask 5.1: Extend [StylesheetTests.cs](../../tests/SpecScribe.Tests/StylesheetTests.cs) (loads the embedded resource from the assembly manifest — edit ONLY `src/SpecScribe/assets/specscribe.css`, the embedded copy; any `docs/live/specscribe.css` is stale [Source: [generate-output-dir-is-specscribeoutput]]): assert the new `--motion-*` tokens exist in `:root`; assert the `no-preference` block references the new insight-surface selectors (heatmap entrance, panel/insight reveal); assert the `reduce` block still exists and neutralizes motion; assert the legend-emphasis rule is present. These follow the existing "cheap guard so a motion seam can't be silently deleted" pattern the 1.5 tests established.
  - [x] Subtask 5.2: **Lock the no-new-JS contract:** add/extend a generation- or asset-level assertion that this story introduced no new script asset and no per-page inline `<script>` for the insight surfaces — the one sanctioned `specscribe.js` remains the ONLY JS ([SiteGeneratorReadmeTests.cs](../../tests/SpecScribe.Tests/SiteGeneratorReadmeTests.cs) already asserts the self-contained script asset; extend that spirit). If Subtask 3.2 did extend `specscribe.js`, assert the legend-emphasis handler is wired but that it stays a class toggle (no navigation/side effects).
  - [x] Subtask 5.3: If a hook class was added to a Charts surface (e.g. the heatmap entrance class), add a small [ChartsTests.cs](../../tests/SpecScribe.Tests/ChartsTests.cs) render assertion that the class is emitted, mirroring how 1.5's Charts tests guard the heatmap headline / future-day muting.
  - [x] Subtask 5.4: Run the targeted suite then a real generation pass and eyeball the result (see Testing Requirements). Confirm entrances play once and are gone under `reduce`, legend emphasis works by hover AND keyboard, and nothing loops.

### Review Findings

- [x] [Review][Decision] Donut legends never got the interactive-legend emphasis / keyboard reachability the spec explicitly names — Subtask 3.1 says "Hovering/focusing a **sunburst or donut** legend entry emphasizes the matching-status segments," and Subtask 3.3's `tabindex="0"` requirement applies to legend spans generally. Only `Charts.SunburstLegend` + the `.sunburst-panel:has(...)` CSS shipped; the donut legend markup in `HtmlTemplater.cs:393-398` and `:613-619` (and `EpicsTemplater.cs`, `RequirementsTemplater.cs`) is unchanged plain `<span>`s with no status class and no `tabindex`, and there is no `.donut-panel:has(...)` rule anywhere in `specscribe.css`. Task 3 and Subtasks 3.1/3.3 are checked off `[x]` despite this gap. — **Resolved:** implemented now. Added `Charts.DonutLegend(...)` mirroring `SunburstLegend`'s pattern (`dn-<status>-item` + `tabindex="0"`), wired into all 4 donut-legend call sites (`HtmlTemplater.cs` x2, `EpicsTemplater.cs`, `RequirementsTemplater.cs`), and a matching `.donut-and-legend:has(...) .donut-seg:not(...)` CSS block. Added `.donut-seg`'s missing `opacity` transition so the dim rides `--motion-fast` like the sunburst does. Guarded by `ChartsTests.DonutLegend_EntriesAreKeyboardReachableAndStatusKeyedForEmphasis` and `StylesheetTests.Stylesheet_HasDonutInteractiveLegendEmphasisRule`. Verified live: donut legend spans render `dn-<status>-item`/`tabindex="0"` on the generated dashboard; full suite green (530).

- [x] [Review][Patch] Legend-emphasis `:has()` rules key off `:focus` instead of `:focus-visible` [src/SpecScribe/assets/specscribe.css:1188-1216] — a mouse click on a legend `<span tabindex="0">` leaves it focused, so the dimming effect sticks with no visible indicator until focus moves elsewhere. It also creates rule-priority ambiguity: if one legend item keeps stale `:focus` while another is `:hover`, the two `:has()` rules have equal specificity and source order decides which status's segments stay emphasized, which can show the wrong one. — **Fixed:** all sunburst emphasis rules changed from `:focus` to `:focus-visible` (and the new donut rules were written with `:focus-visible` from the start), so a mouse click no longer sticks the dim. `StylesheetTests.Stylesheet_HasInteractiveLegendEmphasisRule` updated to match.

- [x] [Review][Patch] `Charts.SunburstLegend`'s `label` parameter is interpolated into markup without the `Html()` escaping helper used everywhere else in Charts.cs [src/SpecScribe/Charts.cs:227-241] — harmless today since every call site passes a hardcoded literal, but inconsistent with the file's convention and a latent gap if the helper is ever reused with non-literal input. — **Fixed:** wrapped in `Html(label)`; `DonutLegend` was written with the same escaping from the start.

- [x] [Review][Patch] Stale/misleading CSS comment claims `.refinement-funnel` "isn't built yet" and the sibling-reveal rule is "a harmless no-op" for it [src/SpecScribe/assets/specscribe.css:2514-2536] — but `Charts.RefinementFunnel` (Story 3.6) already renders `<div class="refinement-funnel">` on the live dashboard (wired via `HtmlTemplater.cs:279-281`), so the entrance/reduce rules are already active on real markup, not a no-op. Comment (and the Completion Notes claim "no funnel markup exists yet") needs updating to reflect that 3.6 has since landed. Confirmed live during review verification: `.refinement-funnel` renders and is visible in the generated dashboard screenshot. — **Fixed:** comment updated to note 3.6 has landed and the keying is no longer a no-op.

- [x] [Review][Patch] Two test-coverage gaps in this diff: `Sunburst_LegendEntriesAreKeyboardReachableAndStatusKeyedForEmphasis` only exercises the epic-level `Sunburst` overload, leaving the story-level and task-level `SunburstLegend` call sites (different tuples, including the 2-item pending/done set) unverified; and `CommitHeatmap_CellsCarryStaggerColumnIndexForEntrance` only asserts `--col:0` appears once, which would also pass if `--col` were hardcoded to `0` for every cell — it doesn't prove the stagger index actually differs across columns. — **Fixed:** added `EpicSunburst_...` and `TaskSunburst_...` legend tests (the two previously-unpinned call sites), and strengthened the heatmap test to assert more than one distinct `--col` value is emitted. Full suite green (532).

- [x] [Review][Defer] Both epic-level and story-level `Sunburst` call `SunburstLegend` with an identical hardcoded 6-tuple array [src/SpecScribe/Charts.cs] — nothing keeps them in sync if one is edited without the other. Minor maintainability nit, no functional impact today. — deferred, pre-existing style pattern, not blocking

## Dev Notes

- **This is the capstone motion/visual-language story of Epic 3 — it harmonizes, it does not invent surfaces.** Stories 3.1 (git pulse), 3.3 (coverage), 3.4 (tree), and 3.6 (funnel) each *deliberately shipped static* and wrote "motion is Story 3.5's job" into their own task lists [Source: [3-3…:57](../../_bmad-output/implementation-artifacts/3-3-agent-and-workflow-structure-coverage-insights.md), [3-4…:33](../../_bmad-output/implementation-artifacts/3-4-interactive-tree-views-for-project-and-artifact-structure.md)]. Story 3.5's deliverable is a single, tokenized, reduced-motion-safe motion vocabulary applied consistently across all insight surfaces so the portal feels like one deliberate product — impressive on load/demo, bounded and meaningful in daily use. It is NOT a mandate to add new charts or a JS drill engine.
- **⚠️ Sequencing — implement 3.5 LAST in Epic 3.** At authoring time, siblings 3.1–3.4 and 3.6 are all `ready-for-dev` (not `done`), so several surfaces this story animates (coverage panel, tree, funnel, git-pulse panels) **do not exist in the codebase yet** — only the Story 1.5 dashboard charts + the commit heatmap are built. Best path: land 3.1–3.4 and 3.6 first, then this story. If 3.5 is implemented before a sibling lands, write the motion rules **defensively** — key them to shared/opt-in selectors so an absent surface is a harmless no-op, animate only what actually renders (heatmap, sunburst, donuts, progress bars, whichever Epic 3 panels exist), and record any surface-specific motion left for later in Completion Notes. Never fabricate a sibling's markup to animate it. *(This sequencing is flagged to the human at story-creation; confirm ordering before dev.)*
- **The motion contract is TWO paired media blocks — extend them, never rewrite.** Story 1.5 built the `no-preference` entrance block ([:2135](../../src/SpecScribe/assets/specscribe.css)) and left it side-by-side with Story 1.4's `reduce` block ([:2156](../../src/SpecScribe/assets/specscribe.css)) *specifically so this story could extend the pair*. Everything you add hangs off one of those two blocks. The `reduce` half is a hard accessibility invariant (AC #2, UX-DR18/8): every animation you add on the `no-preference` side must be neutralized on the `reduce` side with zero information loss.
- **One motion vocabulary, mirroring the status-token discipline.** The whole point is that "flashy" stops being ad-hoc: today timings are scattered literals (0.9s, 0.6s, 0.12s, 0.15s, 0.3s). Tokenize them (`--motion-*`) so every insight surface animates with the same feel, exactly as `--status-*` made one color-per-stage the single source ([specscribe-status-token-system], [StatusStyles.cs](../../src/SpecScribe/StatusStyles.cs)). Motion tokens are a NEW parallel family — do not reshape or overload the status tokens.
- **Charts are pure SVG + CSS, no JS — and that stays true here.** The deliberate project stance is no client-side chart engine; the ONE sanctioned exception is the small `specscribe.js` (tooltips + copy + menu dismissal) [Source: [charting-is-pure-svg-no-js], [Charts.cs:6-8](../../src/SpecScribe/Charts.cs)]. Prefer pure CSS for all of this story's motion (entrances are `@keyframes` + `animation`; legend emphasis wants scoped `:has()`/sibling selectors). Only if legend emphasis genuinely can't be expressed in CSS may you add a **minimal class-toggle** to the existing `specscribe.js` — no new file, no library, no drill/zoom/hash engine, no dark-mode/theme system (all permanently out of scope per the Story 1.5 guardrails).
- **Visual-polish timings are seed-level; accessibility is not.** The architecture spine is explicit: "Exact visual polish values (timings, shadows, micro-motion tuning) are seed-level as long as shared semantics and accessibility invariants remain intact" [Source: [ARCHITECTURE-SPINE.md#Seed, Not Invariant:103](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md)]. So you have latitude to tune durations/easings to feel right — but the reduced-motion parity, status-never-color-only, and keyboard-reachability invariants are non-negotiable.
- **Insight motion is additive and never owns baseline success.** Per AD-4, insight providers/surfaces enrich output but must never block generation [Source: [ARCHITECTURE-SPINE.md#AD-4:58](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md)]. This is a CSS/asset story, so the risk is low, but keep it true: no motion work should introduce a generation-time failure path, and every surface must render correctly with CSS/JS stripped.
- **Reuse, don't reinvent, the existing hover grammar.** Story 1.5 D1/D3 already defined the interaction grammar (interactive → lift+accent+shadow; data-hover → highlight+tooltip; static → nothing) and the sunburst hover-invert ([specscribe.css:1118](../../src/SpecScribe/assets/specscribe.css)). Legend emphasis (Task 3) must read as the same language — dim the non-matching, emphasize the match — not a novel effect.
- **`docs/live` / generated-output caveat.** Edit only the embedded `src/SpecScribe/assets/specscribe.css` and (if unavoidable) `src/SpecScribe/assets/specscribe.js`; the `docs/live/*` copies are vestigial/gitignored and stale, and `StylesheetTests` loads from the assembly manifest, not disk [Source: [generate-output-dir-is-specscribeoutput]]. The default generate output dir is `SpecScribeOutput`. Remember the Story 1.5 review lesson: asset hrefs are cache-busted by build version ([PathUtil.cs](../../src/SpecScribe/PathUtil.cs) `RenderHeadOpen`), so a CSS/JS change ships without a stale-cache artifact — do not reintroduce un-versioned hrefs.
- **No `architecture.md` exists** — the governing docs are the spec kernel ([SPEC.md](../../_bmad-output/specs/spec-specscribe/SPEC.md), [ARCHITECTURE-SPINE.md](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md)) plus the UX design ([DESIGN.md](../../_bmad-output/planning-artifacts/ux-designs/ux-SpecScribe-2026-07-05/DESIGN.md), [EXPERIENCE.md](../../_bmad-output/planning-artifacts/ux-designs/ux-SpecScribe-2026-07-05/EXPERIENCE.md)). Keep the monolithic `src/SpecScribe` layout — do not introduce the aspirational core/adapter split.

### Project Structure Notes

- **Primary UPDATE:** [src/SpecScribe/assets/specscribe.css](../../src/SpecScribe/assets/specscribe.css) — the `--motion-*` tokens in `:root`; extended `no-preference` entrance rules for the insight surfaces; the legend-emphasis rule; extended `reduce` neutralization. This is the center of gravity for the story.
- **Possible UPDATE (only if a hook is needed):** [src/SpecScribe/Charts.cs](../../src/SpecScribe/Charts.cs) — add an entrance hook class to the heatmap (and/or the funnel if 3.6 lands here) purely so CSS can target it; no geometry/aria changes. [src/SpecScribe/assets/specscribe.js](../../src/SpecScribe/assets/specscribe.js) — ONLY if pure-CSS legend emphasis is infeasible (minimal class toggle).
- **TEST:** [tests/SpecScribe.Tests/StylesheetTests.cs](../../tests/SpecScribe.Tests/StylesheetTests.cs) (motion tokens, no-preference/reduce coverage, legend-emphasis rule), [ChartsTests.cs](../../tests/SpecScribe.Tests/ChartsTests.cs) (any new hook class), and a no-new-JS guard (extend the [SiteGeneratorReadmeTests.cs](../../tests/SpecScribe.Tests/SiteGeneratorReadmeTests.cs) self-contained-asset assertion).
- **No new files, no new folders, no new project** — this is an in-place extension of already-integrated CSS/render seams, matching every prior Epic 1–3 story.
- **No conflict** between epics.md Story 3.5 ACs, UX-DR20, UX-DR18/8/17, and the DESIGN.md motion philosophy — all specify bounded, purposeful, single-pass, reduced-motion-respecting insight motion with non-color/text redundancy preserved.

### References

- [Source: [epics.md#Story 3.5 (lines 479-497)](../../_bmad-output/planning-artifacts/epics.md)] — user story + both ACs; Epic 3 goal (insight surfaces) and FRs (FR9-FR11, FR14).
- [Source: [epics.md#UX-DR20 (line 103)](../../_bmad-output/planning-artifacts/epics.md)] — "high-impact but purposeful visual polish for insight modules (animated transitions, visual summaries, and drill paths) without violating performance or accessibility constraints."
- [Source: [epics.md#UX-DR18 (line 101)](../../_bmad-output/planning-artifacts/epics.md), [UX-DR8 (line 91)](../../_bmad-output/planning-artifacts/epics.md), [UX-DR17 (line 100)](../../_bmad-output/planning-artifacts/epics.md)] — reduced-motion respect, viewport-triggered progress animation, status never color-only.
- [Source: [DESIGN.md "Motion philosophy" (line 162)](../../_bmad-output/planning-artifacts/ux-designs/ux-SpecScribe-2026-07-05/DESIGN.md)] — purposeful & brief; single-pass entrances; hover opacity shifts; no looping; respect `prefers-reduced-motion`. [Source: [EXPERIENCE.md "Motion" (lines 162, 220-229)](../../_bmad-output/planning-artifacts/ux-designs/ux-SpecScribe-2026-07-05/EXPERIENCE.md)] — the `reduce` collapse and "instant swap, no animated transition" contract.
- [Source: [specscribe.css:2135-2148 (no-preference entrance block), :2156-2172 (reduce block), :1118 (.sb-seg hover), :1478 (.donut-seg), :1464 (.progress-fill)](../../src/SpecScribe/assets/specscribe.css)] — the exact paired motion seam to extend and the hover transitions to tokenize.
- [Source: [Charts.cs:6-8 (pure SVG/CSS no-JS class doc), :468-590 (CommitHeatmap), :127-231 (Sunburst + legend), :48-98 (Donut)](../../src/SpecScribe/Charts.cs)] — the surfaces and legends this story animates; the no-JS convention to uphold.
- [Source: [assets/specscribe.js](../../src/SpecScribe/assets/specscribe.js)] — the ONE sanctioned progressive-enhancement script; extend minimally only if CSS legend emphasis is infeasible.
- [Source: [1-5-dashboard-insight-polish-and-visual-truthfulness.md:56 (D2 entrance), :115 (UXO C3 → 3.5), :189-196 (guardrails)](../../_bmad-output/implementation-artifacts/1-5-dashboard-insight-polish-and-visual-truthfulness.md)] — the entrance block this extends, the deferred interactive-legend item, and the no-drill-engine/no-dark-mode guardrails.
- [Source: [3-4…:33,:62](../../_bmad-output/implementation-artifacts/3-4-interactive-tree-views-for-project-and-artifact-structure.md), [3-3…:57](../../_bmad-output/implementation-artifacts/3-3-agent-and-workflow-structure-coverage-insights.md)] — siblings' explicit hand-off of motion to Story 3.5; the static-surface discipline to now enliven.
- [Source: [ARCHITECTURE-SPINE.md#AD-4 (58-64)](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md), [#Seed, Not Invariant (98-104)](../../_bmad-output/specs/spec-specscribe/ARCHITECTURE-SPINE.md)] — insight motion additive/non-blocking; polish timings are seed-level, accessibility invariants are not.
- [Source: [charting-is-pure-svg-no-js], [specscribe-status-token-system], [story-1-4-a11y-seams-for-1-5], [generate-output-dir-is-specscribeoutput], [tooltip-clipping-use-ss-tooltip-node]] — project memory: pure-CSS/no-JS charts (one small script exists), single status-token source, shared a11y/focus seams to extend, correct output dir + no stale css copy, body-level tooltip node.

### Testing Requirements

- Preserve ALL existing motion/a11y assertions from Stories 1.4 and 1.5 — the `reduce` block, the `no-preference` entrance block, per-status tokens, focus-visible, aria-labels/`<title>`s must not regress.
- Add: `--motion-*` tokens present in `:root`; `no-preference` covers the insight surfaces; `reduce` neutralizes the new motion; legend-emphasis rule present; no new script asset / no per-page inline chart script.
- Run the targeted filter, then a real generation pass:
  - `dotnet test tests/SpecScribe.Tests/SpecScribe.Tests.csproj --filter "FullyQualifiedName~Stylesheet|FullyQualifiedName~Charts|FullyQualifiedName~SiteGenerator|FullyQualifiedName~HtmlTemplater"`
  - `dotnet run --project src/SpecScribe -- generate --source _bmad-output --adrs docs/adrs --output SpecScribeOutput --project-name SpecScribe`
- Manual verification (open the generated `index.html` and any built insight pages): entrances play once and settle (no loop); with OS reduced-motion ON, everything is instant and no information is lost; legend emphasis works by hover AND keyboard focus; motion is smooth (opacity/transform only). Use `py -3` for any BMAD helper scripts on this Windows host.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Claude Opus 4.8)

### Debug Log References

- Created as the Epic 3 capstone motion/visual-language story; consolidates the motion explicitly deferred to it by Stories 1.5 (UXO C3 interactive-legend emphasis), 3.3, and 3.4.
- Sequencing flag raised at creation: siblings 3.1–3.4 and 3.6 are `ready-for-dev` (not `done`), so implement 3.5 last or write motion rules defensively against absent surfaces.
- Environment: use `py -3` for BMAD helper scripts on this Windows host.

### Completion Notes List

**One tokenized motion vocabulary (Task 1).** Added a new `--motion-*` family to `:root` (`--motion-fast`, `--motion-entrance`, `--motion-entrance-long`, `--motion-ease`, `--motion-stagger`) — a parallel family to `--status-*`, not a reshaping of it. Re-routed the existing Story 1.5 entrances (`progress-grow`, `chart-enter`) and the chart hover transitions (`.sb-seg`, `.donut-seg`, both 0.12s) through the tokens; timings-only, no pixel changes. **Scope decision:** left the generic card hover transitions (`0.15s` border/shadow/lift, ~10 sites) as literals — tokenizing them to `--motion-fast` (0.12s) would have changed their established feel, violating the "do not change the visual result" constraint; they're a distinct interaction from the chart hover feel this story owns.

**Extended entrance motion to Epic 3 surfaces (Task 2).** Heatmap cells fade in (opacity-only — a CSS `transform` on an SVG child honors `transform-box` quirks that would fly cells in from the SVG origin), staggered left-to-right one column at a time via a per-cell `--col` index emitted by `Charts.CommitHeatmap`, with the delay derived in-CSS as `min(calc(var(--motion-stagger) * var(--col)), 0.4s)` — token-driven and capped, single-pass. Sibling panels (`.git-pulse`, `.coverage`, `.structure-tree`, `.refinement-funnel`) get a gentle rise+fade `panel-reveal`, keyed to already-present root classes so an unbuilt surface is a harmless no-op. Verified in the generated site that `.git-pulse` and `.coverage` render and animate.

**Funnel (3.6) deferred, rule pre-wired.** Story 3.6 has not landed, so no `.refinement-funnel` markup exists yet. Per the story's defensive-sequencing guidance, the sibling-reveal and reduce-neutralization rules are already keyed to `.refinement-funnel` (currently a no-op); when 3.6 ships, its staged per-stage narrowing reveal can hang off that same keying. No funnel markup was invented here.

**Interactive-legend emphasis, pure CSS (Task 3).** The item Story 1.5 deferred (UXO C3). Factored the three sunburst legends (`Sunburst`, `EpicSunburst`, `TaskSunburst`) into one `Charts.SunburstLegend` helper that emits each entry with a status class (`sb-<status>-item`) and `tabindex="0"`. Emphasis is pure CSS via `.sunburst-panel:has(.sb-<status>-item:hover/:focus) .sb-seg:not(.sb-<status>) { opacity: 0.28 }` — the same "dim the siblings, emphasize the target" grammar as the existing `.sunburst:hover`. **No JS was added** (the sanctioned `specscribe.js` is untouched). The dim rides `--motion-fast` and collapses to instant under reduced motion; the always-visible swatch + label + segment `<title>`/aria keep status readable without the affordance.

**Reduced-motion invariant (Task 4).** Every new entrance lives inside the `no-preference` block, so it's already inert under `reduce`; the `reduce` block additionally cancels them explicitly (`animation: none` on the heatmap cells + sibling panels) as belt-and-suspenders and, critically, to kill the heatmap cells' inline `animation-delay`, which `fill-mode: both` would otherwise honor by holding a cell invisible through its delay. With motion off every surface is fully present at rest — zero information loss. All animated properties are compositor-friendly (`opacity`/`transform`); no `will-change` was needed. Every prior a11y seam (focus-visible ring, aria-labels/`<title>`s, skip link, single `<main>`, progressbar ARIA, JS-free `<title>` tooltip) is untouched.

**Verification.** Full suite green (510 tests, incl. the new Stylesheet/Charts guards). Real generation pass succeeded (40 pages); confirmed in the shipped output that the `--motion-*` tokens ship, heatmap cells carry `--col`, the 6 legend items carry `sb-<status>-item` + `tabindex`, `.git-pulse`/`.coverage`/`.structure-tree` render, and the CSS href stays build-versioned/cache-busted. Drove the preview browser: heatmap/donut cells settle to opacity 1 after the single-pass reveal (identical to the pre-existing 1.5 donut pattern), and the `:has()` legend-emphasis mechanism dims non-matching segments to 0.28 and reverts cleanly (verified via a class proxy — the preview window isn't OS-focused, so real `:hover`/`:focus` can't fire there, an environment artifact, not a CSS defect).

### File List

- `src/SpecScribe/assets/specscribe.css` — added the `--motion-*` token family; tokenized the existing entrance + chart-hover timings; added the pure-CSS interactive-legend emphasis rule + focusable-legend focus ring; extended the `no-preference` block with the heatmap cell stagger and sibling-panel reveal; extended the `reduce` block to neutralize the new insight motion.
- `src/SpecScribe/Charts.cs` — factored the three sunburst legends into `SunburstLegend(...)` emitting status-keyed, `tabindex="0"` entries; emitted a per-cell `--col` stagger index on heatmap cells.
- `tests/SpecScribe.Tests/StylesheetTests.cs` — guards for the motion tokens, token-routed entrances, no-preference insight coverage, reduce-block neutralization, the legend-emphasis rule, and the no-legend-emphasis-in-JS contract (with `@media`-block brace-matching helpers).
- `tests/SpecScribe.Tests/ChartsTests.cs` — render assertions for the status-keyed/focusable legend entries and the heatmap `--col` stagger hook.

### Change Log

- 2026-07-08 — Implemented Story 3.5 (Epic 3 capstone). One tokenized `--motion-*` vocabulary; bounded single-pass entrances extended to the heatmap (staggered cells) and sibling insight panels (git pulse, coverage, structure tree; funnel pre-wired/deferred to 3.6); pure-CSS interactive-legend emphasis via `:has()` (UXO C3, no new JS); reduced-motion invariant preserved with explicit neutralization. Tests: +6 Stylesheet guards, +2 Charts assertions. Full suite green (510). Status → review.
