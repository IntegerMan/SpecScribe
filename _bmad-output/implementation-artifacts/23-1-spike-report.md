# Story 23.1 Spike Report — Nuxt-over-IR Feasibility

**Status:** Complete · **Date:** 2026-07-23 · **Branch:** `spike/nuxt-ir-23-1` · **Probe:** [`spike/nuxt-ir/`](../../spike/nuxt-ir/README.md)

This is the durable deliverable of Story 23.1 (the throwaway probe code is deletable with the branch). It mirrors
Story 22.1's report structure. **It does NOT author a new ADR:**
[ADR 0009](../../docs/adrs/0009-frontend-framework-for-projection-layer.md) already decided the direction (Vue +
Nuxt 3, universal/SSR, charts stay C#-SVG in the IR); this spike de-risks the *build* of that decision with real
numbers, and **gates** whether Stories 23.2–23.5 proceed as scoped or are re-scoped.

**Nothing found here contradicts ADR 0009.** No amendment is proposed. Two findings change the *shape* of the
implementation stories, and one is a defect in already-shipped code.

---

## Context

ADR 0009 (Accepted, 2026-07-20) ratified the direction but explicitly **did not schedule the build** — it seated
Epic 23 as backlog and ruled the epic must "open with a spike" proving the four integration risks it could not
settle by argument [§Disposition #2, §"Remaining spike-owned unknowns"]:

1. **NFR6 survives** — does `nuxt generate` actually produce a JS-optional-navigable static baseline?
2. **Parity is achievable** — can one representative surface match the golden output?
3. **Webview CSP survives** — can Nuxt's hydration run under `script-src 'nonce-…'` with no `unsafe-inline`?
4. **Node-in-pipeline cost** — what does a Node build step cost against the self-contained-binary model?

### Method

The probe is a real Nuxt 3 app rendering **four real SpecScribe surfaces** from a **proxy IR**. Epic 22's canonical
IR (Story 22.2) is still `backlog`, so — exactly as Story 22.1 did — the spike consumes the already-shipped
`SpaDelivery` output (`spa/manifest.json` + `spa/pages-*.json`, 82.3 MB / 35 files / 914 pages) generated from this
repo with `specscribe generate --spa`. All SpaDelivery-specific knowledge is confined to one file
([`ir/adapter.mjs`](../../spike/nuxt-ir/ir/adapter.mjs)) so the finding survives 22.2's schema change.

Surfaces were chosen so that between them they exercise all three AC #2 sub-risks — no single existing surface
does (the dashboard is chart/token-heavy but thin on prose; prose pages are the reverse):

| route | IR page | probes |
| --- | --- | --- |
| `/dashboard` | `index.html` | chart-SVG injection (148 inline SVGs), `--status-*`/`--motion-*` tokens |
| `/adr-0006` | `adrs/0006-delivery-architecture-and-distribution.html` | Mermaid ×5, gherkin, capability, tables |
| `/story-22-1` | `implementation-artifacts/22-1-spike-…html` | reference chips ×34, comment annotations |
| `/ux-design` | `planning-artifacts/ux-designs/…/DESIGN.html` | colour swatches ×48, tables |

Charts and analysis stayed in C# throughout (ADR 0009 non-goal; ADR 0008 axis C not reopened). Machine: Windows 11,
Node v24.11.1, npm 11.6.2, Nuxt 3.

**Provenance of the numbers (corrected 2026-07-23 after code review).** An earlier draft claimed "every number
below is reproducible with `npm run ir && npm run generate && npm run measure`". That was not true, and the
distinction matters for anyone re-deriving these figures:

| figures | provenance |
| --- | --- |
| Axis 1 per-surface table; Axis 2 `<main>` byte columns, verbatim check and Markdig renderer counts; the Axis 3 script inventory; the 4-surface weight aggregate | **Harness-derived** — [`scripts/measure.mjs`](../../spike/nuxt-ir/scripts/measure.mjs) recomputes them, and writes `measurements/measurements.json` |
| Axis 3 CSP matrix (the four policy rows) | **Session-measured** in a live browser via [`scripts/csp-probe.mjs`](../../spike/nuxt-ir/scripts/csp-probe.mjs); read off network outcomes and DOM counts, not scripted |
| Axis 4 wall-clock timings (`npm ci`, all `nuxt generate` runs) and the full-site (918-route) weight table | **Session-measured**; not reproduced by any committed script |

The `<main>`-region extraction, the golden-gate normalization (footer clock, `?v=<ModuleVersionId>`, product
version, CRLF/BOM) and the renderer counts were added to `measure.mjs` during review — the original run computed
them ad hoc, and the committed check was a weaker substring-containment test. The lockfile and
`measurements/measurements.json` are no longer gitignored; the originals were lost with the spike worktree and
must be regenerated by a re-run.

---

## Measured Evidence

### Axis 1 — NFR6 under `nuxt generate`

Per surface, from the emitted static files. **"js-off" is the same document with every `<script>…</script>`
removed** — the question NFR6 actually asks:

| route | prerendered HTML | with all `<script>` removed | inline `<svg>` | `<a href>` |
| --- | --- | --- | --- | --- |
| `/dashboard` | 366,910 B | **345,264 B** | 148 | 570 |
| `/adr-0006` | 59,966 B | **59,307 B** | 54 | 98 |
| `/story-22-1` | 73,291 B | **72,626 B** | 54 | 121 |
| `/ux-design` | 53,360 B | **52,698 B** | 54 | 67 |

(The `<svg>`/`<a href>` columns are counted on the whole js-off **document**, chrome included, which is why the
dashboard shows 148 SVGs here but 115 in the `<main>`-scoped parity table below. Same page, different region.)

No blank shell, no `<div id="app">`-only output.

**Confirmed live in a real browser, not just by inspecting files.** Serving the output under the shipped webview
CSP (which blocks every Nuxt module — see Axis 3) the page rendered **148 SVGs** with no Vue app instance
attached: the real dashboard, drawn with no framework JavaScript running.

**Navigability — what was and was not proven (corrected 2026-07-23 after code review).** Routes are files, not a
client router: the spike index emits literal `<a href="/dashboard">` in the prerendered HTML and every target
exists as its own `index.html` on disk (verified for all four). But that covers **the spike's own index links
only**. The migrated surface's own ~570 in-content links were **not** resolved and do **not** resolve against
this route space — the IR's hrefs are `code/…`, `adrs/…` while the routes are `/dashboard`, `/adr-0006`, …
That mismatch is the same one that makes `crawlLinks: true` abort the build (finding 8): it is not merely a
crawler quirk, it is the link graph failing to resolve. Mapping the IR's own link graph onto Nuxt routes is
**Story 23.3 scope** and is the remaining work before "navigable without JS" can be claimed for a real surface.

> **Verdict: NFR6 holds for rendering.** Prerender produces a fully-rendered, JS-optional baseline for real
> surfaces — file-per-route, no hydration shell, confirmed with all framework JS blocked. Full in-content
> navigability is **not** yet proven and depends on 23.3's route mapping.

### Axis 2 — Parity against the golden output

The parity oracle is the shipped static page. Measured on the `<main>` region — the substantive content — with
the surrounding chrome reported separately, because the two differ for a reason that has nothing to do with Nuxt:

| route | golden `<main>` | IR `<main>` | Nuxt `<main>` | golden ≡ IR | Nuxt ≡ IR |
| --- | --- | --- | --- | --- | --- |
| `/dashboard` | 349,439 B | 349,162 B | 349,162 B | ✗ (277 B) | **✓ byte-identical** |
| `/adr-0006` | 31,163 B | 31,163 B | 31,163 B | **✓** | **✓ byte-identical** |
| `/story-22-1` | 45,103 B | 45,103 B | 45,103 B | **✓** | **✓ byte-identical** |
| `/ux-design` | 25,143 B | 25,143 B | 25,143 B | **✓** | **✓ byte-identical** |

**Vue's `v-html` round-trips the IR content string verbatim** — the exact IR byte sequence appears unmodified in
the emitted HTML for all four surfaces (`emitted.includes(irContentHtml)` = true). No re-serialization, no
attribute reordering, no self-closing-tag rewriting. This was the single largest parity risk and it is simply
absent.

**What this does and does not measure (added 2026-07-23 after code review).** The surface is rendered as a single
`v-html` injection inside a thin Vue shell ([`SurfaceShell.vue`](../../spike/nuxt-ir/components/SurfaceShell.vue)),
and the visual fidelity comes from importing the shipped `specscribe.css` wholesale. So this measures **injection
fidelity — that host-rendered HTML survives the trip through Nuxt intact** — not that a Vue reimplementation
reproduces the surface. That is deliberate (charts and prose stay C#-rendered per ADR 0009), but it carries a
condition that must not be lost:

> The finding "the ~889 LOC of custom Markdig renderers are not a fidelity risk" holds **only if the canonical
> IR carries Markdig-rendered prose HTML as strings.** ADR 0008 §Decision #1 defines the IR as "AD-2's
> host-neutral view models plus pre-rendered SVG chart fragments" — view models, not rendered prose. The proxy
> IR this spike consumed (`SpaDelivery`) happens to carry whole rendered HTML, which is why the risk evaporated.
> **If Story 22.2 builds the canonical IR to ADR 0008's letter, this finding does not transfer and the
> renderer-fidelity risk returns in full.** This is now a hard requirement handed to 22.2 (see Follow-ups).

Custom Markdig renderer output survives with **identical counts to the golden page** on every surface:

| surface | Mermaid | ref chips | comment annot. | swatches | gherkin | capability | tables | inline SVG |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/dashboard` | — | — | — | 35 = 35 | 9 = 9 | — | — | 115 = 115 |
| `/adr-0006` | 5 = 5 | — | — | — | 1 = 1 | 1 = 1 | 3 = 3 | — |
| `/story-22-1` | — | 34 = 34 | 1 = 1 | — | — | — | — | — |
| `/ux-design` | — | — | — | 48 = 48 | — | — | 5 = 5 | — |

**Enumerated differences (all three are IR-capture artefacts, not Nuxt artefacts):**

1. **Dashboard `<main>`, 277 B — the IR drops 5 anchors, 3 of them `code/*.html` links.** The shipped static
   dashboard hyperlinks the git-pulse bar labels
   (`<a href="code/tests/…/SiteGeneratorAdapterTests.cs.html">`); the `SpaDelivery` capture of the same panel
   emits them as plain text. Golden `<main>` has 553 anchors, the IR has 548. This is a **lossy capture in
   already-shipped code**, reproducible without Nuxt, and it is a defect in its own right (see Follow-ups).
2. **Nav chrome outside `<main>`.** The static ADR page renders a page-local context band
   (`site-nav-local-context`, `aria-label="ADRs"`); the SPA capture carries the generic key-views nav instead.
   Known static-vs-SPA divergence in the shipped delivery form.
3. **Document `<head>`.** Nuxt owns the head; it emits `<title>` + the stylesheet link but not the golden's
   og/description meta, favicon data-URI, or `?v=<ModuleVersionId>` cache-bust. Trivially closable in 23.3 (a head
   projection from the IR) — not attempted here, since the head is 23.3's job, not the spike's.

Scoped CSS behaved as ADR 0009 assumed, with **one ergonomic surprise worth scoping into 23.2**: `<style scoped>`
compiles to `[data-v-*]` attribute selectors that Vue stamps only on **template-authored** elements. It does
**not** reach `v-html`-injected markup — styling the IR's own content from an SFC requires `:deep()` or a global
sheet. Since the migration's entire premise is injecting host-rendered HTML, this affects every component 23.2
writes.

Token binding is drift-free by construction: the probe imports the **shipped `specscribe.css` verbatim** rather
than re-typing values, and the scoped component's `var(--status-done)` resolved live to `rgb(107, 143, 98)` in
the browser.

> **Verdict: parity is achievable — for content that travels as rendered HTML.** Content parity is
> byte-identical through the whole chain; the only content delta is caused by a pre-existing SpaDelivery capture
> defect, and the head delta is ordinary 23.3 scope. **Conditional on the IR carrying rendered prose HTML** (see
> the box above) and on the IR being lossless, which today it is not — the dashboard's 277 B delta is exactly
> that losslessness failing. Byte and count parity were measured; no rendered/visual comparison and no
> interactive-behaviour check (pure-CSS tab strips, `:target` deep links, client sort/filter) was performed.

### Axis 3 — Webview CSP

The webview ships (`WebviewRenderAdapter.cs:113` — the story text's `:102` was a line-number slip inherited
through the spec, probe header, README and Debug Log; the policy *string* below is byte-verbatim and correct):

```
default-src 'none'; base-uri 'none'; form-action 'none'; img-src __CSP_SOURCE__ data: https:;
style-src 'unsafe-inline' __CSP_SOURCE__; script-src 'nonce-__NONCE__'; font-src __CSP_SOURCE__ data:;
```

**Can Nuxt emit a nonce? Yes.** Nuxt 3 core has no nonce option, but Nitro's `render:html` hook exposes the
emitted markup before serialization, and a ~10-line plugin stamps `nonce="__NONCE__"` onto **all three** scripts
Nuxt emits (entry module, the `window.__NUXT__` config inline script, and the `__NUXT_DATA__` block). Because a
prerendered build is static, the nonce cannot be per-render by construction — but placeholder-plus-substitution
is *exactly* the mechanism `WebviewRenderAdapter` already uses, so this composes with the existing host.

**But a nonce is not sufficient.** Replaying the shipped policy verbatim over the real output in a real browser
(`scripts/csp-probe.mjs`, which substitutes `__NONCE__` per request the way the host does):

| policy | `_payload.json` | module chunks | hydrated | rendered content |
| --- | --- | --- | --- | --- |
| **shipped webview policy** | blocked | **blocked** | no | **intact** — 148 SVGs |
| + `'strict-dynamic'` | blocked | ok | yes | **BLANKED — 0 SVGs** |
| + `'strict-dynamic'` + `connect-src` | ok | ok | yes | intact — 148 SVGs |
| payload inlined + `'strict-dynamic'` | n/a | ok | yes | intact — 148 SVGs |

Three things this measures that argument would have missed:

- **A nonce does not propagate to a module's static imports.** The nonced entry module loads; the three chunks it
  imports are blocked. `'strict-dynamic'` is the only thing that fixes it.
- **`connect-src` is absent, so `default-src 'none'` blocks the payload fetch.** Nuxt preloads
  `/dashboard/_payload.json` via `<link rel="preload" as="fetch">` and fetches it on hydration.
- **The partial relaxation is catastrophically worse than no relaxation.** With `'strict-dynamic'` but no
  `connect-src`, hydration *runs*, `useAsyncData` finds no payload, `v-if="data"` goes false, and Vue **deletes
  the prerendered content** — 148 SVGs to zero, on a page that rendered perfectly with JS fully blocked. Whoever
  touches this policy must change both knobs or neither.

  **Probe-architecture caveat (added 2026-07-23 after code review).** The *blanking* is amplified by this
  spike's own shape: the page fetches its content through `/api/ir` via `useAsyncData` and guards the render
  with `v-if="data"` ([`pages/[...surface].vue:13`](../../spike/nuxt-ir/pages/[...surface].vue)). A production
  component that receives its content as a prop, inlines the payload, or renders through `<NuxtIsland>` would
  not delete the DOM this way. What generalises is the underlying hazard — **a half-applied CSP relaxation lets
  hydration start and then fail**, and a hydration failure after mount can destroy prerendered content. The
  specific 148→0 magnitude is this probe's, not Nuxt's. 23.4's regression test should assert content survival
  against *its own* component shape rather than assuming this one.

`experimental.payloadExtraction: false` removes the fetch entirely (payload inlined into the HTML), leaving
`'strict-dynamic'` as the **single** required CSP change. Verified working.

One un-nonced inline script remains, and it is **not Nuxt's**: the IR content itself carries SpecScribe's own
`<script type="application/json" id="sunburst-explorer-data">` island (20,915 B, from Story 20.2). The webview
already strips that island today — confirming that whatever consumes the IR, not the framework, owns this
problem.

> **Verdict: hydration does NOT survive the webview CSP as shipped — but the gap is precisely two lines wide and
> fully characterised.** Adding `'strict-dynamic'` to `script-src` plus disabling payload extraction is sufficient
> and was verified end-to-end. Degradation under the unchanged policy is *graceful*: content renders, only
> interactivity is lost.

**Honesty boundary (mirroring ADR 0005 / Story 6.3; widened 2026-07-23 after code review):** this is a real
Chromium engine enforcing the byte-identical policy string over the real emitted output. Four things it is *not*:

1. Not VS Code's Electron webview, and not `vscode-resource:` URIs. The actual paint needs one manual F5 and is
   **not** claimed here.
2. **Delivery mechanism differs.** The probe sends the policy as an HTTP **response header**; the webview ships
   it in a `<meta http-equiv="Content-Security-Policy">` tag ([`WebviewRenderAdapter.cs:113`](../../src/SpecScribe/WebviewRenderAdapter.cs)).
   Meta-delivered CSP is not applied to resources requested before the tag is parsed, and ignores some
   directives outright — exactly the class of difference that governs whether a `<link rel="preload" as="fetch">`
   is blocked. The matrix is evidence about the **policy string**, not about meta-tag delivery.
3. **Resource delivery differs.** The probe serves `_nuxt/*.js` and `*.css` over HTTP from a real origin that
   `__CSP_SOURCE__` resolves to. The real webview has no server: it inlines CSS into `<style>` blocks and hands
   the host one self-contained document. Whether Nuxt's `_nuxt/` asset graph can be delivered through
   `vscode-resource:` **at all** — a question that precedes any CSP question — was not tested.
4. Consequently **"the gap is two lines wide" is a lower bound**, established for the policy string in a
   server-delivered context. It is not a full characterisation of the webview integration.

**Security note (added 2026-07-23 after code review) — the recommended fix contradicts an ADR 0005 property.**
ADR 0005 records as a property of the webview document: *"The body carries no scripts of its own — the only
`<script>` in each document is the shim's own nonce'd script"* [ADR 0005 §"What the shim guarantees"]. Nuxt
hydration breaks that by construction, and `'strict-dynamic'` changes the policy's meaning: today only nonced
scripts execute; under `'strict-dynamic'` any nonced script may load arbitrary further scripts, with the
allow-list directives ignored. That relaxation lands on a surface rendering HTML derived from **the user's own
repository markdown**, and the IR already carries an embedded `<script>` island the webview strips by hand.

This does **not** contradict ADR 0009 (which never spoke to the webview's CSP), so no ADR 0009 amendment is
proposed. But the ADR-amendment trigger was originally evaluated only against 0009, which missed this.
**Story 23.4 owns proposing the ADR 0005 amendment** at the point it actually makes the CSP change — with the
threat analysis (what the current policy protects against, and what is given up) as part of that proposal
rather than as an afterthought. Flagged explicitly rather than buried in a story note, per the Epic 10 retro's
ADR-trigger action item.

### Axis 4 — Node in the pipeline

**Today (state a):** Node is **build-time-only and developer-side**. `extension/` (4 devDeps) and
`tools/prism-vendor/` (1 devDep) both carry zero runtime deps, and nothing in `src/SpecScribe/*.csproj` references
node or npm. **`specscribe generate` does not need Node to run.**

**Measured cost of adding it:**

| | measured |
| --- | --- |
| `npm ci`, cold cache | **18.4 s** |
| `node_modules` | **183.9 MB** (build-time only) |
| `nuxt generate`, first ever run | 112.5 s |
| `nuxt generate`, warm, 4 routes | 22.8 – 26.7 s |
| `nuxt generate`, 204 routes | 30.6 s |
| **`nuxt generate`, all 918 routes** | **37.1 s** (prerender phase alone: 15.5 s) |
| marginal cost per route | ≈ 17 – 24 ms |
| fixed Vite/Nitro build floor | ≈ 22 s |
| `_nuxt/` client assets | 239 KB |

Scaling is benign — the cost is dominated by a ~22 s fixed build, and the site's pages add only ~15 s. For
reference, the full `specscribe generate --spa` pipeline (parse, git analysis, charts, IR emission) takes
**64.5 s** on the same machine, so a **warm** Nuxt build adds roughly **+57 % to generation wall-clock**.

**Two corrections to this section (2026-07-23, after code review):**

- **Route-count reconciliation.** The figures mix three different counts, and an earlier draft relabelled one run
  from "355 pages" (Debug Log) to "914 pages" (here) without reconciling them. Correctly: `manifest.pages` = **914**
  entries; the C# static site emits **915** HTML files; the Nuxt build prerendered **918** routes. That 918 was
  inflated by a probe bug — `SPIKE_SCALE` re-sampled the four named surfaces that were already routes, so four
  pages were prerendered **twice** (fixed in `extract-ir.mjs`: it now excludes the named surfaces and
  stride-samples instead of taking an alphabetical prefix). The 64.5 s pipeline run and the 37.1 s Nuxt run cover
  the same site either way, so the +57 % ratio stands; the page labels attached to it did not.
- **The headline uses the WARM number; the cold path is ~3× worse.** First-ever `nuxt generate` was **112.5 s**,
  and `npm ci` adds **18.4 s** on top — neither is inside the +57 %. That matters more than it first looks:
  finding 9 establishes prerendering is inherently **per-project**, so if Nuxt ever runs on a user's machine the
  **cold** path is the user-facing one, and a fair end-user figure is closer to **130 s** than 37 s. The warm
  figure is the right one only for the CI/package-time variant (b).

**Output weight at full site scale:**

| | bytes |
| --- | --- |
| C# static site (915 HTML files) | **57.0 MB** |
| Nuxt prerendered HTML (921 files) | **56.5 MB** — within 1 % |
| Nuxt hydration payloads (919 files) | **+67.4 MB** |
| **Nuxt total** | **129 MB (2.26×)** |

The prerendered HTML is **not** the cost — it is essentially identical to what C# emits. The entire 2.26× is
hydration payload: every page's content is shipped twice, once as HTML and once as devalue-serialized JSON.
Inlining the payload does not reduce the total (dashboard HTML goes 366,910 B → 827,584 B), it only moves it.
The lever exists — Nuxt server components / `<NuxtIsland>` render server-side without a hydration payload, which
is the right shape for content that is static by construction — but it was **not** measured here and should be
23.2's first experiment.

**Scope caveat on the 2.26× (added 2026-07-23 after code review).** This measures **the probe's data path**, not
Nuxt in general. Content is fetched through `/api/ir` with `useAsyncData`, and anything reaching a component via
an async data source is serialized into `_payload.json` **by construction** — so this is the
payload-*maximising* shape. The honest claim is: *an Epic 23 implementation that routes IR content through
async data will roughly double the site's weight; the `<NuxtIsland>`/server-component shape is expected to
avoid it and is unmeasured.* An earlier draft generalised this to "**any** Epic 23 implementation story that
does not address payload duplication doubles the site's weight", which the evidence does not support.

**Distribution impact (input to 23.5, not a solution):**

- **(b) Nuxt at package/CI time, pre-built assets shipped in the binary** — compatible with the self-contained
  model. The 239 KB of `_nuxt/` assets embed like any other asset; `npm ci` + `nuxt build` join CI, matching how
  `extension/` and `tools/prism-vendor/` already work. End users still need no Node. **This is the only variant
  that preserves ADR 0005/0006.**
- **(c) Node in the `specscribe generate` critical path or end-user runtime** — breaks the self-contained model.
  Prerendering is per-project (routes come from *the user's* IR), so shipping pre-built HTML is impossible; the
  user's machine would need Node **plus a ~184 MB `node_modules` and a Nuxt build toolchain**.

  **Correction (2026-07-23, after code review) — which channels actually break.** An earlier draft said "neither
  the npx channel (16.8) nor the VS Code Marketplace channel (16.5) could stay Node-runtime-free." That is
  wrong: **both already require Node by construction.** ADR 0005 states "the extension host runs in VS Code's
  Node.js", and ADR 0006 §C describes npx as "a ~1.5 KB npm wrapper … resolves and spawns the self-contained
  binary" — i.e. invoked *by* npm, on Node. Restated correctly:

  | channel | Node already present? | what variant (c) would actually cost |
  | --- | --- | --- |
  | **standalone self-contained binary** (ADR 0005 §2, Story 16.3) | **no** | This is the channel (c) genuinely breaks — the binary would have to carry or require a Node runtime plus `node_modules`. |
  | **npx** (Story 16.8) | **yes** (npm runs it) | Not Node's *presence* — the cost is shipping/fetching ~184 MB of build toolchain. Because Node is already there, per-project prerender is **more** feasible here than the earlier framing implied. |
  | **VS Code Marketplace** (Story 16.5) | **yes** (extension host) | Same: the constraint is bundle weight and toolchain, not runtime availability. |

  So the real currency is **toolchain weight and cold build time (~130 s, see above)**, not "does the user have
  Node". This sharpens 23.5's problem rather than dissolving it — the per-project prerender tension below is
  unaffected — but 23.5 should evaluate the npx channel on its merits instead of ruling it out.

**The unresolved tension 23.5 must answer:** ADR 0009 wants Nuxt to render *the user's* project, but (b) only
ships pre-built assets, not pre-rendered pages. Either the shipped artefact becomes a client-rendered SPA over the
IR (which forfeits NFR6 — the thing Axis 1 just proved), or `specscribe generate` invokes Node at run time
(which forfeits self-containment). **This spike did not solve it, and it is the single biggest open question in
Epic 23.**

---

## Findings

1. **NFR6 holds for rendering under Nuxt prerender.** Fully-rendered, JS-optional, file-per-route. Confirmed in a
   real browser with all framework JS blocked. **In-content navigability is not yet proven** — the IR's own link
   graph does not resolve against Nuxt routes (finding 8 is the same problem), which is 23.3 scope.
   *(AC #1 — satisfied for the rendering baseline; navigability claim narrowed.)*
2. **Content parity is byte-identical, for content that travels as rendered HTML.** `v-html` round-trips the IR
   verbatim; `<main>` is byte-for-byte equal golden → IR → Nuxt on 3 of 4 surfaces, and every custom Markdig
   renderer survives at identical counts. The ~889 LOC of custom renderers ADR 0009 named as the fidelity risk
   are **not** a risk when their output travels as data — **conditional on the canonical IR actually carrying
   rendered prose HTML**, which ADR 0008 does not currently specify (see Axis 2 and Follow-ups).
   *(AC #2 — satisfied; measures injection fidelity, not a Vue reimplementation.)*
3. **The dashboard's 277-byte delta is a pre-existing SpaDelivery defect**, not a migration cost: the SPA capture
   drops 5 anchors (3 `code/*.html` links) from the git-pulse panel. **Root cause** (identified in code review):
   [`SiteGenerator.cs:2979`](../../src/SpecScribe/SiteGenerator.cs) passes `codeItemHref: CodeItemHref` into
   `HtmlTemplater.BuildIndexPage` for the static site; the SPA path (`:2806`) and the **webview** path (`:2539`)
   both omit the argument, so `Charts.GitPulsePanel(pulse, null)` → `CodeItemLink(path, null)` degrades the bar
   labels to plain text. The webview surface is affected identically — not just the SPA.
4. **`<style scoped>` does not reach `v-html` content.** Needs `:deep()`. Affects every component 23.2 writes.
5. **The webview CSP blocks hydration**, a nonce alone is insufficient (module imports need `'strict-dynamic'`),
   and **the half-fix blanks the page**. Two-knob fix verified. Characterised **for the policy string in a
   header-delivered, HTTP-served context** — not for `<meta>` delivery or `vscode-resource:` assets, so the
   two-line gap is a lower bound. The recommended relaxation also **contradicts an ADR 0005 property** (see the
   security note in Axis 3); 23.4 owns proposing that amendment.
6. **Nuxt emits nonces fine** via a Nitro `render:html` hook, composing with the host's existing `__NONCE__`
   substitution. The one un-nonced script is SpecScribe's own IR-carried island. (The plugin originally covered
   `head`/`bodyPrepend`/`bodyAppend` only; `html.body` is now mapped too, so a server-component script could not
   slip through uncounted. Inventory taken on the dashboard page.)
7. **Node costs +37 s warm / ~130 s cold, and 2.26× output weight** for the async-data shape — the weight being
   *entirely* hydration payload, with the prerendered HTML within 1 % of C#'s. The `<NuxtIsland>` shape that
   would avoid the payload is unmeasured.
8. **`crawlLinks: true` is unusable.** Nitro's crawler follows the injected IR content's own links and aborts the
   build on the first 404. The route table must come from the IR manifest — which is the correct design anyway.
9. **Packaging is unresolved and is the epic's real risk.** Prerendering is inherently per-project; "ship
   pre-built assets" does not cover it.

## Gate — do Stories 23.2–23.5 proceed as scoped?

| Story | Gate | Reason |
| --- | --- | --- |
| **23.2** Component library & design-token bridge | **PROCEED, re-scoped** | Direction confirmed (tokens bind cleanly, scoped CSS compiles as expected). Add two items the spike surfaced: (a) a `:deep()`/global-sheet convention for styling `v-html`'d IR content — finding 4; (b) **measure server-components/`<NuxtIsland>` against payload duplication first**, since finding 7 says the naive shape doubles the site. |
| **23.3** Migrate baseline surfaces (dashboard, epics) | **PROCEED as scoped** | Parity is byte-identical on the surface it targets. Add to its scope: the `<head>` projection (difference 3), and **route-mapping the IR's own in-content link graph** — the unproven half of AC #1's navigability claim, and the reason `crawlLinks` is unusable. Note that this spike proved *injection* fidelity, not a component reimplementation, so 23.3 is where scoped-CSS Vue components first get genuinely exercised. |
| **23.4** Migrate remaining surfaces, retire `HtmlRenderAdapter` | **PROCEED, gated** | Must not start until 23.5 answers packaging — retiring the C# renderer is irreversible, and finding 9 means the delivery model for a Nuxt-rendered site is not yet settled. Also owns the webview CSP change (finding 5) as a **two-knob atomic edit**, with a regression test asserting content survives, since the half-applied fix is worse than none. |
| **23.5** Packaging reconciliation (Node build step) | **RE-SCOPE — promote and resequence ahead of 23.4** | It is no longer a tidy-up at the end of the epic; finding 9 makes it the epic's load-bearing unknown. It must decide between per-project prerender-at-generate-time (Node in the critical path) and a client-rendered shipped SPA (forfeits NFR6) **before** 23.4 retires the C# renderer that is currently the only thing satisfying both. **Scope it on corrected premises** (see Axis 4): npx and the Marketplace channel already run on Node, so the real currency is toolchain weight (~184 MB) and cold build time (~130 s), not Node's availability — and the npx channel deserves evaluation on its merits rather than exclusion. |

**ADR 0009 stands unamended.** Nothing measured contradicts it: NFR6 survives prerender, parity is achievable,
charts-as-data works, and the CSP gap is closable. The re-scoping above is about *sequence and emphasis*, not
direction.

## Follow-ups outside this story

- **`SpaDelivery` drops `code/*.html` links from the dashboard's git-pulse panel** (finding 3). A live fidelity
  defect in shipped code today — the SPA and webview surfaces silently lose those links. Fix standalone, or fold
  into Story 22.2 when it generalises the capture.
- **What the front end wished the IR carried** (input to Story 22.2): a structured `head`/meta projection (title,
  description, og tags) so the consumer need not re-derive it; the page-local nav context that the static
  renderer computes but the capture discards; and a declaration of which embedded `<script>` islands a consumer
  must strip or nonce.
- **HARD REQUIREMENT for Story 22.2 (owner-directed 2026-07-23, from code review): the canonical IR must carry
  Markdig-rendered prose HTML as strings.** This spike's central finding — that the ~889 LOC of custom Markdig
  renderers are not a fidelity risk — is true *only because* the proxy IR (`SpaDelivery`) carries whole rendered
  HTML. ADR 0008 §Decision #1 specifies the IR as "AD-2's host-neutral view models plus pre-rendered SVG chart
  fragments"; if 22.2 builds to that letter and ships view models instead of rendered prose, **the finding does
  not transfer and the renderer-fidelity risk returns in full**, along with the ~4,691 LOC templater
  reimplementation Epic 23 assumes it can avoid. 22.2 must either carry rendered prose HTML or explicitly
  re-open this risk.
- **In-content link resolution is unproven and belongs to 23.3.** The IR's own hrefs (`code/…`, `adrs/…`) do not
  resolve against Nuxt's route space. Until they are mapped, "navigable without JS" is claimed only for the
  spike's index, not for a real surface.

---

## What was NOT done (scope guard held)

No component library or token-convention work (23.2). No production dashboard/epics migration (23.3). The C#
`HtmlRenderAdapter` is fully intact (23.4). No packaging solution (23.5) — only its cost, as AC #3 requires. No
chart port and no analysis moved out of C#. **No production code shipped:** `src/SpecScribe/**` and `tests/**` are
git-confirmed untouched; everything the spike produced lives under `spike/nuxt-ir/`. Nothing joins
`SpecScribe.slnx` (still exactly two projects), `dotnet build src/SpecScribe`, `dotnet pack`, or the `extension/`
bundle, and `node_modules/` is excluded by the generator's own directory filter
([`SiteGenerator.cs:4110`](../../src/SpecScribe/SiteGenerator.cs)).

**One correction to the story's stated invariant, recorded rather than glossed:** the task text asked that "the
generated site stay byte-identical with or without the spike folder." **It does not, and it could not have for
any spike in this repo** — SpecScribe documents its own repository, so adding files to it changes its
self-documentation. Measured: the spike folder adds **13 code pages** (`code/spike/nuxt-ir/**`), which is the
same thing `code/spike/vscode/**` and `code/spike/delivery/**` already do for the Story 6.3 and 6.6 spikes. What
*is* true, and is the invariant that actually matters, is that **the generator's rendering behaviour is
unchanged**: no `src/` or `tests/` file was modified.

**Correction (2026-07-23, after code review):** an earlier draft also offered "the `GoldenContentFingerprint`
gate produces the exact same hash with and without this branch" as evidence. That experiment is **structurally
vacuous** — the test builds its site from a synthetic `Directory.CreateTempSubdirectory` fixture
([`SiteGeneratorAdapterTests.cs:19`](../../tests/SpecScribe.Tests/SiteGeneratorAdapterTests.cs)) and never walks
the repository, so adding `spike/nuxt-ir/` could not have changed its hash under any circumstance. The claim was
true but proved nothing. The load-bearing evidence is simply that **no `src/` or `tests/` file was modified**,
which `git` confirms directly.

### Test suite

`dotnet test SpecScribe.slnx -c Release` → **2,169 passed, 3 skipped, 1 failed**. The single failure is
`GenerateAll_GoldenContentFingerprint_IsStableAfterNormalizingVolatileTokens`, expecting an updated constant of
`1eaefe51a347154b158b652ec5baa0660d05a9f1a4dc9224f200e0ade3a5482b`. **This is pre-existing stale-constant drift,
not caused by this spike** — verified by running the same test on a clean detached worktree of `9243de5` with the
spike folder absent, which fails with the *byte-identical* expected hash. It is the same class of drift Story
22.1 reported on `main`.

An earlier run of the same suite also showed a transient failure in
`SiteGeneratorGitInsightsTests.GenerateAll_TwoRunsProduceIdenticalHubMarkup` (missing `git-insights.html` in a
temp dir) which did **not** reproduce on re-run — flaky, and noted here rather than buried.

---

## AC coverage

_Mirrors Story 22.1's report structure (this section was missing from the first draft)._

| AC | verdict | evidence | boundary |
| --- | --- | --- | --- |
| **#1** NFR6 JS-optional baseline holds under Nuxt prerender | **satisfied, narrowed** | Axis 1 table (harness-derived); live browser render with all Nuxt modules CSP-blocked; file-per-route on disk | Proves the baseline **renders** without JS. **Navigability proven only for the spike's own index** — the surface's in-content links do not resolve against Nuxt routes (23.3 scope). "JS disabled" was approximated by script-stripping + a CSP-blocked load, not by a browser JS-off toggle. |
| **#2** Visual + functional parity for one representative surface | **satisfied, conditional** | Axis 2 `<main>` byte comparison under golden-gate normalization; `v-html` verbatim round-trip on all 4; Markdig renderer counts equal on every surface; tokens bound from the shipped stylesheet | Measures **injection fidelity**, not a Vue reimplementation — the surface is one `v-html` div over the shipped CSS. **Conditional on the canonical IR carrying rendered prose HTML** (hard requirement now handed to 22.2). No visual/screenshot diff and no interactive-behaviour check was performed; parity is byte + count only. Three enumerated deltas, all IR-capture artefacts. |
| **#3** Webview-CSP verdict + Node-in-pipeline cost reported, no production code shipped | **satisfied** | Axis 3 CSP matrix (session-measured, live browser, policy string byte-verbatim); Axis 4 timings and weights; `git` confirms `src/**` and `tests/**` untouched and `SpecScribe.slnx` unchanged | CSP verdict established for the **policy string** under **header** delivery over an **HTTP-served** asset graph — not `<meta>` delivery, not `vscode-resource:`, not an Electron paint. So "two lines wide" is a lower bound. Node cost headline is the **warm** build; cold is ~3× higher. The distribution-channel analysis was corrected post-review. |

**Gate named** for 23.2–23.5 in the table above — AC #3's final obligation. **No new ADR authored**: nothing
contradicts ADR 0009. One ADR conflict *was* surfaced (ADR 0005's "body carries no scripts of its own", broken
by the recommended `'strict-dynamic'` fix) and is assigned to 23.4 to propose rather than resolved here.
