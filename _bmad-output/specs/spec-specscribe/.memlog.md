---
topic: SpecScribe CLI-first multi-framework static status portal
updated: 2026-07-05T20:47
---

- (direction) Mode interactive; rich structured PRD provided so direct distillation used without guided elicitation.
- (capability) CAP-1 Intent: ingest BMad, Spec Kit, and GSD/GSD-Pi artifacts into one projection model so mixed repositories still render coherently.
- (capability) CAP-1 Success: representative repositories for each supported framework render core planning and tracking views without fatal generation failures.
- (capability) CAP-2 Intent: generate a readable static portal with navigation, dashboards, and traceability links across epics, stories, requirements, and ADRs.
- (capability) CAP-2 Success: generated index links every discovered major artifact class and recognized IDs resolve to stable pages without unresolved-link output.
- (capability) CAP-3 Intent: keep portal output current through reliable watch-mode regeneration during normal edit/save workflows.
- (capability) CAP-3 Success: rapid successive source edits regenerate without corrupting output or causing source-file lock side effects.
- (capability) CAP-4 Intent: compute local insight modules for repository momentum and artifact-structure coverage while preserving baseline generator usability.
- (capability) CAP-4 Success: dashboard shows baseline git pulse and artifact family coverage, while insight failures degrade gracefully and do not block generation.
- (capability) CAP-5 Intent: ship and prioritize a CLI-first surface now, then expose the same shared model in a follow-on VS Code read-only experience when core reliability is stable.
- (capability) CAP-5 Success: zero-required-flag CLI workflows succeed in supported repos and extension work only proceeds after an explicit relevance decision with no unresolved critical reliability regressions.
- (constraint) Adapter architecture must isolate framework parsing from rendering so adding a new framework does not require core templating rewrites.
- (constraint) Generation must degrade gracefully on partial or malformed artifacts, emitting non-fatal notices instead of hard failures for unsupported content.
- (constraint) Baseline generation performance must stay responsive; deeper analytics stay optional and independently toggleable.
- (constraint) Core operation is local-first with no required remote telemetry or hosted control plane.
- (non-goal) Hosted SaaS concerns such as multi-tenant governance, RBAC, and account management are out of scope for this effort.
- (non-goal) SpecScribe will not replace issue trackers, PM suites, or source-control platforms.
- (non-goal) Framework artifact authoring/editing workflows are out of scope; this contract covers readability and insight projection only.
- (assumption) Deep git analytics remain opt-in to protect baseline responsiveness.
- (assumption) Initial VS Code extension release is read-only and local-first before any authoring controls.
- (question) How should coverage tiers (rendered, summarized, unsupported) be communicated so users understand interpretation boundaries?
- (question) Which additional agent-file families should be promoted to plugin candidates after core plus orchestration coverage stabilizes?
- (question) Should deeper git analysis be configured by CLI flag, settings profile, or both?
- (question) Which concrete relevance signals should trigger extension work before further CLI or parser investment?
- (event) Wrapper-only content dropped: process/ceremony framing and document metadata not load-bearing for downstream build and validation decisions.
- (event) Pass 1 Coherence verdict: passes Spec Law 1-6 and 8 with five intent+success capabilities, concrete constraints, explicit non-goals, and testable success signal; assumptions and open questions recorded without invention.
- (event) Pass 2 Preservation verdict: load-bearing PRD claims preserved in SPEC.md or requirements-catalog.md; wrapper-only document metadata and process framing intentionally excluded.
- (event) Spec finalized for slug specscribe with companion requirements-catalog.md and canonical memlog as decision record.
- (direction) User-directed update: all features must be configurable via interactive options or CLI params, including git and ADR coverage, with settings persisted in directory-scoped settings files.
- (constraint) All user-facing features require parity across interactive and CLI configuration surfaces.
- (constraint) Git analytics and ADR coverage controls are mandatory configurable surfaces, not fixed behavior.
- (constraint) Settings persistence is directory-scoped via settings file with overridable run-time parameters.
- (decision) Local-only and read-only posture reaffirmed; IDE integration may add helper buttons that generate prompts/commands but do not mutate artifacts.
- (decision) README communication requirement added: user-facing options must be presented with tables and short descriptions.
- (decision) Support target expanded to current popular spec-driven frameworks/modules with explicit popularity criteria required.
- (decision) Concrete relevance signals defined as measurable extension-go thresholds: demand or friction plus reliability and capacity gates.
- (event) Pass 1 Coherence verdict (update): Spec Law 1-6 and 8 satisfied after preserving CAP IDs and tightening constraints/success signal for configurability and local/read-only posture.
- (event) Pass 2 Preservation verdict (update): new user directions preserved in SPEC.md plus requirements-catalog.md and settings-and-signals.md companion.
- (event) Source sync performed: PRD updated with Alignment Addendum capturing configurability, settings persistence, local/read-only posture, README table guidance, framework coverage policy, and concrete relevance signal definition.
- (decision) Architectural direction clarified: shared projection/rendering core must feed both static HTML and VS Code webview adapters without parser/projection duplication.
- (decision) Extension relevance now explicitly includes user workflow mix: context-switch friction and VS Code-family editor-share determine timing alongside reliability/capacity gates.
- (event) Source sync performed: PRD alignment addendum updated with dual-output architecture and workflow-fit extension relevance criteria.
- (event) Pass 1 Coherence verdict (update): Spec Law compliance maintained while preserving CAP IDs and tightening CAP-5 architecture/success criteria.
- (event) Pass 2 Preservation verdict (update): user directions about context-switch and editor-mix relevance preserved in SPEC companions and PRD addendum.
- (decision) Added rendering-architecture companion defining shared projection/rendering core, host-neutral view models, and HTML/webview delivery adapters.
- (decision) Concrete boundary sketch includes interfaces for artifact adapters, projection pipeline, settings provider, insight providers, renderer, and delivery adapters with parity rules.
- (note) Resolved requirements-catalog inconsistency by removing duplicate workflow-friction signal wording and retaining context-switch/browser/watch-oriented definition.
- (event) Source sync performed: PRD updated with module-boundary sketch section aligned to dual-output architecture.
- (event) Pass 1 Coherence verdict (update): Spec Law maintained, capability IDs preserved, companions expanded for architecture detail without bloating kernel.
- (event) Pass 2 Preservation verdict (update): user-approved request for module boundary sketch preserved in rendering-architecture companion and PRD addendum.
- (decision) Architecture spine to codify one shared projection/rendering core with host-neutral view models and delivery-only HTML/webview adapters, matching the PRD/SPEC dual-surface direction.
- (decision) Companion ADRs requested for docs/adrs to capture shared-core architecture and read-only helper posture for humans and AI context.
- (constraint) Spine must stay local-only, read-only, and settings-driven with directory-scoped persistence and CLI/interactive override parity.
- (event) spine finalized
